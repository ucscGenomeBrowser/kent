# vegaInfo.sql was originally generated by the autoSql program, which also 
# generated vegaInfo.c and vegaInfo.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Vega Genes track additional information
CREATE TABLE vegaInfo (
    transcriptId varchar(255) not null,	# Vega transcript ID
    otterId varchar(255) not null,	# otter (Ensembl db) transcript ID
    geneId varchar(255) not null,	# Vega gene ID
    method varchar(255) not null,	# GTF method field
    geneDesc varchar(255) not null,	# Vega gene description
    confidence varchar(255) not null,	# Vega gene confidence
              #Indices
    PRIMARY KEY(transcriptId)
);
