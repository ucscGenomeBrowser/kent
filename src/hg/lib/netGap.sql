# netGap.sql was originally generated by the autoSql program, which also 
# generated netGap.c and netGap.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#gap portion of net file
CREATE TABLE netGap (
    chrom varchar(255) not null,	# gap on Chrom
    level int unsigned not null,	# Level of alignment
    chromStart int unsigned not null,	# Start of gap
    chromSnd int unsigned not null,	# End of gap
              #Indices
    PRIMARY KEY(chrom)
);
