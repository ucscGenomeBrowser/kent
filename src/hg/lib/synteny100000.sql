# synteny100000.sql was originally generated by the autoSql program, which also 
# generated synteny100000.c and synteny100000.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Mouse Synteny from blastz single coverage
CREATE TABLE synteny100000 (
    chrom varchar(255) not null,	# Human Chrom
    chromStart int unsigned not null,	# Start on Human
    chromEnd int unsigned not null,	# End on Human
    mouseChrom varchar(255) not null,	# Mouse Chromosome
    score int unsigned not null,	# score always zero
    strand char(1) not null,	# + direction matches - opposite
              #Indices
    PRIMARY KEY(chrom)
);
