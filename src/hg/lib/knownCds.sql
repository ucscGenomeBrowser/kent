# knownCds.sql was originally generated by the autoSql program, which also 
# generated knownCds.c and knownCds.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#The CDS part of a genePredExt.
CREATE TABLE knownCds (
    name varchar(255) not null,	# Name of gene
    cdsStartStat varchar(255) not null,	# enum('none','unk','incmpl','cmpl')
    cdsEndStat varchar(255) not null,	# enum('none','unk','incmpl','cmpl')
    exonCount int unsigned not null,	# number of exons
    exonFrames longblob not null,	# Exon frame {0,1,2}, or -1 if no frame for exon
              #Indices
    PRIMARY KEY(name)
);
