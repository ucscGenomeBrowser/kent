# mouseSyn.sql was originally generated by the autoSql program, which also 
# generated mouseSyn.c and mouseSyn.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Synteny between mouse and human chromosomes.
CREATE TABLE mouseSyn (
    chrom varchar(255) not null,	# Name of chromosome
    chromStart int unsigned not null,	# Start in chromosome
    chromEnd int unsigned not null,	# End in chromosome
    name varchar(255) not null,	# Name of mouse chromosome
    segment int not null,	# Number of segment
              #Indices
    PRIMARY KEY(chrom(12),chromStart),
    UNIQUE(chrom(12),chromEnd)
);
