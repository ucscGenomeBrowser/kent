/* array - test some array stuff. */

array of string names = ("Sam", "Mike", "Stef");

foreach string name in names
    {
    print(name);
    }

array of bit bits = (1,0,1,1,0,1,1,1);
foreach bit bit in bits
    {
    prin(bit);
    prin(" ");
    }
print("");

array of int primes = (1,2,3,5,7,11,13);

foreach int i in primes
    {
    prin(i);
    prin(" ");
    }
print("");
