# minGeneInfo.sql was originally generated by the autoSql program, which also 
# generated minGeneInfo.c and minGeneInfo.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Auxilliary info about a gene (less than the knownInfo)
CREATE TABLE minGeneInfo (
    name varchar(255) not null,	# gene accession
    gene varchar(255) not null,	# gene name
    product varchar(255) not null,	# gene product
    note varchar(255) not null,	# gene note
    protein varchar(255) not null,	# gene protein
    gi varchar(255) not null,	# gene genbank id
    ec varchar(255) not null,	# ec number 
    entrezGene varchar(255) not null,	# entrez gene id
              #Indices
    PRIMARY KEY(name)
);
