class person
    {
    string firstName;
    string lastName;
    float age;
    }

para agePerson(person p, float amount)
    {
    person p1,p2;
    (p1,p2) = (p,p);
    p2.age += amount;
    }

person p = ("Jim", "Kent", 45);
agePerson(p, 0.5);
print(p);
