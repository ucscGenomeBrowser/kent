# humanParalog.sql was originally generated by the autoSql program, which also 
# generated humanParalog.c and humanParalog.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Human Paralog
CREATE TABLE humanParalog (
    chrom varchar(255) not null,	# Human Chrom
    chromStart int unsigned not null,	# Start on Human
    chromEnd int unsigned not null,	# End on Human
    name varchar(255) not null,	# Human Gene Prediction - paralog
    score int unsigned not null,	# Human mouse aligment score using Blat
    strand varchar(255) not null,	# Strand on Human
    thickStart int unsigned not null,	# Start on Human
    thickEnd int unsigned not null,	# End on Human
              #Indices
    PRIMARY KEY(chrom)
);
