The ftp won't work
unless the utility ftpasswd exists and 
is on the bash path when mongrel starts
by encodeteam.

see encodeftp.soe.ucsc.edu
cd /cluster/encodeftp/ mounting.

using port 21 for production,
using port 3000, 3001, 3002 for beta galt kate etc instances.

see encodeftp.soe.ucsc.edu:/etc/proftpd.conf

syntax for client:

ftp encodeftp.soe.ucsc.edu 3000

