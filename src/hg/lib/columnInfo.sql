# columnInfo.sql was originally generated by the autoSql program, which also 
# generated columnInfo.c and columnInfo.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Meta data information about a particular column in the database.
CREATE TABLE columnInfo (
    name varchar(255) not null,	# Column name
    type varchar(255) not null,	# Column type, i.e. int, blob, varchar.
    nullAllowed varchar(255) not null,	# Are NULL values allowed?
    key varchar(255) not null,	# Is this field indexed? If so how.
    defaultVal varchar(255) not null,	# Default value filled.
    extra varchar(255) not null,	# Any extra information.
              #Indices
    PRIMARY KEY(name)
);
