# borkPseudoHom.sql was originally generated by the autoSql program, which also 
# generated borkPseudoHom.c and borkPseudoHom.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#stats and protein homologies behind Bork pseudogenes
CREATE TABLE borkPseudoHom (
    name varchar(255) not null,	# Bork pseudogene gene name
    protRef varchar(255) not null,	# String with swissprot or trembl reference 
    description longblob not null,	# Freeform (except for no tabs) description
              #Indices
    PRIMARY KEY(name)
);
