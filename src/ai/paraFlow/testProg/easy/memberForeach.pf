class chair
    {
    string name;
    int wheels;
    array of string wheelNames;
    }
chair as = ("desk", 5, ("w1", "w2", "w3", "w4", "w5"));
print(as);
foreach (string str in as.wheelNames)
    print(str);

