# Local declaration so that local gold.html is picked up.
track gold override
html gold

# Local declaration so that local gap.html is picked up.
track gap override
html gap

searchTable sgpGene
searchType genePred
termRegex chr[0-9MUXn]+_[GJv0-9_]+(\.[0-9]+)?
searchPriority 50

searchTable geneid
searchType genePred
searchMethod prefix
termRegex chr[0-9MUXn]+_[GJv0-9_]+(\.[0-9]+)?
searchPriority 50

include trackDb.chainNet.ra
