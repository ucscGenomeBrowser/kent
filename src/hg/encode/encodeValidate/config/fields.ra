# Fields (columns) in ENCODE metadata files

# Naming conventions:
#   Name:  arbitrarily assigned
#   REF:   must come from controlled vocabulary

field files
required yes
happy no

field view
required yes

field Raw accession
required no

field labVersion
required no

field	accession
required	no
