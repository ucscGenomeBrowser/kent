/* mafRef.c was originally generated by the autoSql program, which also 
 * generated mafRef.h and mafRef.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "mafRef.h"

void mafRefStaticLoad(char **row, struct mafRef *ret)
/* Load a row from mafRef table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{
int sizeOne,i;
char *s;

ret->chrom = row[0];
ret->chromStart = sqlUnsigned(row[1]);
ret->chromEnd = sqlUnsigned(row[2]);
ret->extFile = sqlUnsigned(row[3]);
ret->offset = sqlLongLong(row[4]);
}

struct mafRef *mafRefLoad(char **row)
/* Load a mafRef from row fetched with select * from mafRef
 * from database.  Dispose of this with mafRefFree(). */
{
struct mafRef *ret;
int sizeOne,i;
char *s;

AllocVar(ret);
ret->chrom = cloneString(row[0]);
ret->chromStart = sqlUnsigned(row[1]);
ret->chromEnd = sqlUnsigned(row[2]);
ret->extFile = sqlUnsigned(row[3]);
ret->offset = sqlLongLong(row[4]);
return ret;
}

struct mafRef *mafRefLoadAll(char *fileName) 
/* Load all mafRef from a tab-separated file.
 * Dispose of this with mafRefFreeList(). */
{
struct mafRef *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[5];

while (lineFileRow(lf, row))
    {
    el = mafRefLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct mafRef *mafRefCommaIn(char **pS, struct mafRef *ret)
/* Create a mafRef out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new mafRef */
{
char *s = *pS;
int i;

if (ret == NULL)
    AllocVar(ret);
ret->chrom = sqlStringComma(&s);
ret->chromStart = sqlUnsignedComma(&s);
ret->chromEnd = sqlUnsignedComma(&s);
ret->extFile = sqlUnsignedComma(&s);
ret->offset = sqlLongLongComma(&s);
*pS = s;
return ret;
}

void mafRefFree(struct mafRef **pEl)
/* Free a single dynamically allocated mafRef such as created
 * with mafRefLoad(). */
{
struct mafRef *el;

if ((el = *pEl) == NULL) return;
freeMem(el->chrom);
freez(pEl);
}

void mafRefFreeList(struct mafRef **pList)
/* Free a list of dynamically allocated mafRef's */
{
struct mafRef *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    mafRefFree(&el);
    }
*pList = NULL;
}

void mafRefOutput(struct mafRef *el, FILE *f, char sep, char lastSep) 
/* Print out mafRef.  Separate fields with sep. Follow last field with lastSep. */
{
int i;
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->chrom);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%u", el->chromStart);
fputc(sep,f);
fprintf(f, "%u", el->chromEnd);
fputc(sep,f);
fprintf(f, "%u", el->extFile);
fputc(sep,f);
fprintf(f, "%u", el->offset);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

