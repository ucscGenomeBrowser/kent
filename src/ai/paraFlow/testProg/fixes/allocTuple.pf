
class namedNum
    {
    string name;
    int num;
    to init(string name, int num)
        {
	self.name = name;
	self.num = num;
	}
    }

to printNn(namedNum nn)
    {
    print(nn.name + ':' + nn.num);
    }

printNn(("tres",3),);
