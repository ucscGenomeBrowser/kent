# barChartCategory.sql was originally generated by the autoSql program, which also 
# generated barChartCategory.c and barChartCategory.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#bar chart track category information
CREATE TABLE barChartCategory (
    id int unsigned not null,	# internal id
    name varchar(255) not null,	# short identifier
    label varchar(255) not null,	# descriptive label
    color int unsigned not null,	# user assigned color
              #Indices
    PRIMARY KEY(id)
);
