# est3.sql was originally generated by the autoSql program, which also 
# generated est3.c and est3.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#EST 3-prime ends
CREATE TABLE est3 (
    chrom varchar(255) not null,	# Chromosome
    chromStart int unsigned not null,	# Start position in chromosome
    chromEnd int unsigned not null,	# End position in chromosome
    strand char(1) not null,	# + or - strand
    estCount int unsigned not null,	# Number of ESTs supporting this
              #Indices
    INDEX(chrom(12),chromStart)
);
