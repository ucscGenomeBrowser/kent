# cnpIafrate2.sql was originally generated by the autoSql program, which also 
# generated cnpIafrate2.c and cnpIafrate2.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#CNP data from Iafrate lab
CREATE TABLE cnpIafrate2 (
    bin smallint not null,		# for browser speedup
    chrom varchar(255) not null,	# Reference sequence chromosome or scaffold
    chromStart int unsigned not null,	# Start position in chrom
    chromEnd int unsigned not null,	# End position in chrom
    name varchar(255) not null,		# BAC name
    normalGain int unsigned not null,	# Number of normal individuals with gain
    normalLoss int unsigned not null,	# Number of normal individuals with loss
    patientGain int unsigned not null,	# Number of patients with gain
    patientLoss int unsigned not null,	# Number of patients with loss
    total int unsigned not null,	# Total gain and loss
    cohortType varchar(255) not null	# {Control},{Patient},{Control and Patient}
);
