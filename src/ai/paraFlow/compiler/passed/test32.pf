/* array - test some array stuff. */

to zero()
{
array of int zero =  ();
}

to one()
{
array of int one = (1);
}

to two()
{
array of int two = (1,2);
}

array of int nine = (1,2,3,4,5,6,7,8,9);

zero();
one();
two();
two();
complex();

to complex()
{
array of int three = (1,2,3);
array of int four = (1,2,3,4);
array of int a3 = three;
a3 = three;
}

