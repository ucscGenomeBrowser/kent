/* gencodePubMed.h was originally generated by the autoSql program, which also 
 * generated gencodePubMed.c and gencodePubMed.sql.  This header links the database and
 * the RAM representation of objects. */

#ifndef GENCODEPUBMED_H
#define GENCODEPUBMED_H

#define GENCODEPUBMED_NUM_COLS 2

extern char *gencodePubMedCommaSepFieldNames;

struct gencodePubMed
/* Gencode metadata table of PubMed identifier of published experimental data associated with Gencode Genes */
    {
    struct gencodePubMed *next;  /* Next in singly linked list. */
    char *transcriptId;	/* GENCODE transcript identifier */
    int pubMedId;	/* PubMed identifier of supporting literature */
    };

void gencodePubMedStaticLoad(char **row, struct gencodePubMed *ret);
/* Load a row from gencodePubMed table into ret.  The contents of ret will
 * be replaced at the next call to this function. */

struct gencodePubMed *gencodePubMedLoad(char **row);
/* Load a gencodePubMed from row fetched with select * from gencodePubMed
 * from database.  Dispose of this with gencodePubMedFree(). */

struct gencodePubMed *gencodePubMedLoadAll(char *fileName);
/* Load all gencodePubMed from whitespace-separated file.
 * Dispose of this with gencodePubMedFreeList(). */

struct gencodePubMed *gencodePubMedLoadAllByChar(char *fileName, char chopper);
/* Load all gencodePubMed from chopper separated file.
 * Dispose of this with gencodePubMedFreeList(). */

#define gencodePubMedLoadAllByTab(a) gencodePubMedLoadAllByChar(a, '\t');
/* Load all gencodePubMed from tab separated file.
 * Dispose of this with gencodePubMedFreeList(). */

struct gencodePubMed *gencodePubMedCommaIn(char **pS, struct gencodePubMed *ret);
/* Create a gencodePubMed out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodePubMed */

void gencodePubMedFree(struct gencodePubMed **pEl);
/* Free a single dynamically allocated gencodePubMed such as created
 * with gencodePubMedLoad(). */

void gencodePubMedFreeList(struct gencodePubMed **pList);
/* Free a list of dynamically allocated gencodePubMed's */

void gencodePubMedOutput(struct gencodePubMed *el, FILE *f, char sep, char lastSep);
/* Print out gencodePubMed.  Separate fields with sep. Follow last field with lastSep. */

#define gencodePubMedTabOut(el,f) gencodePubMedOutput(el,f,'\t','\n');
/* Print out gencodePubMed as a line in a tab-separated file. */

#define gencodePubMedCommaOut(el,f) gencodePubMedOutput(el,f,',',',');
/* Print out gencodePubMed as a comma separated list including final comma. */

/* -------------------------------- End autoSql Generated Code -------------------------------- */

#endif /* GENCODEPUBMED_H */

