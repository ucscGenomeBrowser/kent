# dvXref2.sql was originally generated by the autoSql program, which also 
# generated dvXref2.c and dvXref2.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#A xref table between SWISS-PROT variant ids and OMIM.
CREATE TABLE dvXref2 (
    varId varchar(255) not null,	# SWISS-PROT variant
    extSrc varchar(255) not null,	# external source (MIM for example)
    extAcc varchar(255) not null,	# external accession
              #Indices
    KEY(varId),
    KEY(extAcc)
);
