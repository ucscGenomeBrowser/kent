# gtexTissue.sql was originally generated by the autoSql program, which also 
# generated gtexTissue.c and gtexTissue.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#GTEx tissue information
CREATE TABLE gtexTissue (
    id int unsigned not null,	# internal id
    name varchar(255) not null,	# short UCSC identifier
    description varchar(255) not null,	# GTEX tissue type detail
    organ varchar(255) not null,	# GTEX tissue collection area
    color int unsigned not null,	# GTEX assigned color
    abbrev varchar(255) not null,	# GTEX tissue abbreviation
              #Indices
    PRIMARY KEY(id)
);
