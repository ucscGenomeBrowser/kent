# doc2.sql was originally generated by the autoSql program, which also 
# generated doc2.c and doc2.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#A solid three dimensional object
CREATE TABLE polyhedron (
    faceCount int not null,	# Number of faces
    faces longblob not null,	# List of faces
    pointCount int not null,	# Number of points
    points longblob not null,	# Array of points
              #Indices
    PRIMARY KEY(faceCount)
);
