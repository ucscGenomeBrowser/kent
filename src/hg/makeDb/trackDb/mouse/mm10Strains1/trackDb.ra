# Local declaration so that local gold.html is picked up.
track gold override
html gold

# Local declaration so that local gap.html is picked up.
track gap override
html gap

track mm10Locations
shortLabel Reference
longLabel Mappings to the mouse reference GRCm38 assembly
group map
priority 30
visibility pack
type bed 4 .
url /cgi-bin/hgTracks?db=mm10&position=$$
