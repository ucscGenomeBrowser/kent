table snpSeq
"Information about sequences contained in rs_fasta files described in snpExtFile"
    (
    string name;        "rsId of sequence"
    string chrom;       "Separate file per chrom"
    int file_offset;	"byte offset of sequence in file"
    )

