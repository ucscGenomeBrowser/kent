# knownToSuper.sql was originally generated by the autoSql program, which also 
# generated knownToSuper.c and knownToSuper.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Map protein superfamilies to known genes
CREATE TABLE knownToSuper (
    gene varchar(255) not null,	# Known gene ID
    superfamily int not null,	# Superfamily ID
    start int not null,	# Start of superfamily domain in protein (0 based)
    end int not null,	# End (noninclusive) of superfamily domain
    eVal float not null,	# E value of superfamily assignment
              #Indices
    INDEX(gene(10)),
    INDEX(superfamily)
);
