int z = 999;
for (int lev5=0; lev5<2; ++lev5)
    {
    for (int outer=0; outer<2; ++outer)
	{
	_printInt(outer);
	for (int middle=0; middle<3; ++middle)
	    {
	    _printInt(middle+100);
	    int sum=0;
	    for (int i=0; i<10; ++i)
		sum += i;
	    _printInt(sum);
	    for (int j=0; j<3; ++j)
		 _printInt(j+1000);
	    }
	}
    }
_printInt(z);
