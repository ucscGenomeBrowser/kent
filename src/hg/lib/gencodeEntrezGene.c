/* gencodeEntrezGene.c was originally generated by the autoSql program, which also 
 * generated gencodeEntrezGene.h and gencodeEntrezGene.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodeEntrezGene.h"



char *gencodeEntrezGeneCommaSepFieldNames = "transcriptId,entrezGeneId";

void gencodeEntrezGeneStaticLoad(char **row, struct gencodeEntrezGene *ret)
/* Load a row from gencodeEntrezGene table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->entrezGeneId = sqlSigned(row[1]);
}

struct gencodeEntrezGene *gencodeEntrezGeneLoad(char **row)
/* Load a gencodeEntrezGene from row fetched with select * from gencodeEntrezGene
 * from database.  Dispose of this with gencodeEntrezGeneFree(). */
{
struct gencodeEntrezGene *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->entrezGeneId = sqlSigned(row[1]);
return ret;
}

struct gencodeEntrezGene *gencodeEntrezGeneLoadAll(char *fileName) 
/* Load all gencodeEntrezGene from a whitespace-separated file.
 * Dispose of this with gencodeEntrezGeneFreeList(). */
{
struct gencodeEntrezGene *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileRow(lf, row))
    {
    el = gencodeEntrezGeneLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeEntrezGene *gencodeEntrezGeneLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodeEntrezGene from a chopper separated file.
 * Dispose of this with gencodeEntrezGeneFreeList(). */
{
struct gencodeEntrezGene *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodeEntrezGeneLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeEntrezGene *gencodeEntrezGeneCommaIn(char **pS, struct gencodeEntrezGene *ret)
/* Create a gencodeEntrezGene out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodeEntrezGene */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->entrezGeneId = sqlSignedComma(&s);
*pS = s;
return ret;
}

void gencodeEntrezGeneFree(struct gencodeEntrezGene **pEl)
/* Free a single dynamically allocated gencodeEntrezGene such as created
 * with gencodeEntrezGeneLoad(). */
{
struct gencodeEntrezGene *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freez(pEl);
}

void gencodeEntrezGeneFreeList(struct gencodeEntrezGene **pList)
/* Free a list of dynamically allocated gencodeEntrezGene's */
{
struct gencodeEntrezGene *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodeEntrezGeneFree(&el);
    }
*pList = NULL;
}

void gencodeEntrezGeneOutput(struct gencodeEntrezGene *el, FILE *f, char sep, char lastSep) 
/* Print out gencodeEntrezGene.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->entrezGeneId);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

