# omimTitle.sql was originally generated by the autoSql program, which also 
# generated omimTitle.c and omimTitle.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#omim long names
CREATE TABLE omimTitle (
    omimId int not null,	# OMIM ID
    type char(1) not null,	# Type (*, ^ or #)
    geneSymbol varchar(255) not null,	# Gene
    title varchar(255) not null,	# title
              #Indices
    PRIMARY KEY(omimId)
);
