kentSrc = ../../../..
A = cdwUnlockSubmittedFile
preMyLibs += ../lib/$(MACHTYPE)/cdwlib.a
include $(kentSrc)/inc/userApp.mk

# clean out the old script version since we have new c version now.
clean::
	rm -f ${SCRIPTS}/${A}.csh
