# encodeRenamerLog.sql was originally generated by the autoSql program, which also 
# generated encodeRenamerLog.c and encodeRenamerLog.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

# Hacked by galt to use datetime

#Log encode rename activity
CREATE TABLE encodeRenamerLog (
    id int unsigned not null auto_increment, # auto-increment item ID
    oldObj varchar(255) not null, # Original Object Name
    newObj varchar(255) not null, # New Object Name
    user varchar(255) not null,   # User who did it
    `when` datetime not null,     # When did this rename happen - hack to datetime in .sql
    state varchar(255) not null,  # What state is it in? start, error, done
    errors longblob not null,     # Log warn, errnoWarn, errAbort
    changes longblob not null,   # Log changes such as file or table renames
    notes longblob not null,      # Additional information
              #Indices
    PRIMARY KEY(id)
);
