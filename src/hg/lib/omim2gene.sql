# omim2gene.sql was originally generated by the autoSql program, which also 
# generated omim2gene.c and omim2gene.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#OMIM omim2gene
CREATE TABLE omim2gene (
    omimId int unsigned not null,	# MIM ID
    entryType varchar(40),	# entry type (gene or phenotype)
    geneId int unsigned,	# ENTREZ gene ID
    approvedGeneSymbol varchar(40),	# Approved Gene Symbol
              #Indices
    KEY `omimId` (`omimId`),
    KEY `geneId` (`geneId`)
) ENGINE=MyISAM DEFAULT CHARSET=latin1;
