# dvBed.sql was originally generated by the autoSql program, which also 
# generated dvBed.c and dvBed.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#core disease variation data
CREATE TABLE dvBed (
    bin smallint not null,
    chrom varchar(255) not null,	# Chromosome
    chromStart int unsigned not null,	# Start position in chrom
    chromEnd int unsigned not null,	# End position in chrom
    name varchar(255) not null,	# BAC Name
              #Indices
    INDEX chrom (chrom(13),bin),
    INDEX name (name(16)),
    INDEX chrom_2 (chrom(13),chromStart)
);
