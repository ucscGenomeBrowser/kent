# Fields (columns) in ENCODE metadata files

# Naming conventions:
#   Name:  arbitrarily assigned
#   REF:   must come from controlled vocabulary

field File Name
required yes
happy no

field Part 
required no

field Dataset Name
required yes

field Assembly REF
required yes

field Data Type REF
required yes

field Raw Data Acc REF
required no

field Data Version
required no

