int ourId = 0;

to nextId() into (int id)
{
ourId += 1;
id = ourId;
}

to seeMe(string me = "Jim", int id = nextId())
{
print(me + " #" +id);
}

// seeMe("Uggy", 3, 44);
seeMe("Wugga", 3);
seeMe("Bugga");
seeMe();
