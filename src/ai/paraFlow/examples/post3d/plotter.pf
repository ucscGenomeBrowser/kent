
global interface plotter
// This is our graphic output interface. We don’t know how
// to draw much, but this will be enough to make wireframe
// three-dimensional shapes.
    {
    to line(double x1,y1,x2,y2);
    to circle(double xCen,yCen, radius);
    }

