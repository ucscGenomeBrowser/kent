# geneBands.sql was originally generated by the autoSql program, which also 
# generated geneBands.c and geneBands.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Band locations of known genes
CREATE TABLE geneBands (
    name varchar(255) not null,	# Name of gene - hugo if possible
    mrnaAcc varchar(255) not null,	# RefSeq mrna accession
    count int not null,	# Number of times this maps to the genome
    bands longblob not null,	# List of bands this maps to
              #Indices
    INDEX(name(8)),
    INDEX(mrnaAcc(10))
);
