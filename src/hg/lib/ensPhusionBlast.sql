# ensPhusionBlast.sql was originally generated by the autoSql program, which also 
# generated ensPhusionBlast.c and ensPhusionBlast.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Ensembl PhusionBlast Synteny (bed 6 +).
CREATE TABLE ensPhusionBlast (
    bin smallint not null,	# Bin number for browser speedup
    chrom varchar(255) not null,	# $Organism chromosome
    chromStart int unsigned not null,	# Start position in $Organism chromosome
    chromEnd int unsigned not null,	# End position in $Organism chromosome
    name varchar(255) not null,	# Name of $Xeno chromosome
    score int unsigned not null,	# Integer alignment score
    strand char(1) not null,	# Direction of alignment: + or -
    xenoStart int unsigned not null,	# Start position in $Xeno chromosome
    xenoEnd int unsigned not null,	# End position in $Xeno chromosome
              #Indices
    INDEX(chrom(16),bin)
);
