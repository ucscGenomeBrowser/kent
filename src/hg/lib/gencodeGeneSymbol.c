/* gencodeGeneSymbol.c was originally generated by the autoSql program, which also 
 * generated gencodeGeneSymbol.h and gencodeGeneSymbol.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodeGeneSymbol.h"



char *gencodeGeneSymbolCommaSepFieldNames = "transcriptId,symbol,geneId";

void gencodeGeneSymbolStaticLoad(char **row, struct gencodeGeneSymbol *ret)
/* Load a row from gencodeGeneSymbol table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->symbol = row[1];
ret->geneId = row[2];
}

struct gencodeGeneSymbol *gencodeGeneSymbolLoad(char **row)
/* Load a gencodeGeneSymbol from row fetched with select * from gencodeGeneSymbol
 * from database.  Dispose of this with gencodeGeneSymbolFree(). */
{
struct gencodeGeneSymbol *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->symbol = cloneString(row[1]);
ret->geneId = cloneString(row[2]);
return ret;
}

struct gencodeGeneSymbol *gencodeGeneSymbolLoadAll(char *fileName) 
/* Load all gencodeGeneSymbol from a whitespace-separated file.
 * Dispose of this with gencodeGeneSymbolFreeList(). */
{
struct gencodeGeneSymbol *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileRow(lf, row))
    {
    el = gencodeGeneSymbolLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeGeneSymbol *gencodeGeneSymbolLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodeGeneSymbol from a chopper separated file.
 * Dispose of this with gencodeGeneSymbolFreeList(). */
{
struct gencodeGeneSymbol *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodeGeneSymbolLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeGeneSymbol *gencodeGeneSymbolCommaIn(char **pS, struct gencodeGeneSymbol *ret)
/* Create a gencodeGeneSymbol out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodeGeneSymbol */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->symbol = sqlStringComma(&s);
ret->geneId = sqlStringComma(&s);
*pS = s;
return ret;
}

void gencodeGeneSymbolFree(struct gencodeGeneSymbol **pEl)
/* Free a single dynamically allocated gencodeGeneSymbol such as created
 * with gencodeGeneSymbolLoad(). */
{
struct gencodeGeneSymbol *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freeMem(el->symbol);
freeMem(el->geneId);
freez(pEl);
}

void gencodeGeneSymbolFreeList(struct gencodeGeneSymbol **pList)
/* Free a list of dynamically allocated gencodeGeneSymbol's */
{
struct gencodeGeneSymbol *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodeGeneSymbolFree(&el);
    }
*pList = NULL;
}

void gencodeGeneSymbolOutput(struct gencodeGeneSymbol *el, FILE *f, char sep, char lastSep) 
/* Print out gencodeGeneSymbol.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->symbol);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->geneId);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

