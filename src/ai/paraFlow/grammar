
classHead: "class" NAME
	;

classDef: classHead '{' '}'
	;

collectionType:  "array" | "list" | "dir" | "tree" | "pointer" | classType
	;

compositeType:  collectionType "of" type
	| collectionType
	;

type: "var" | "byte" | "short" | "int" | "long" | "float" | "double" | "string" | collectionType | definedType
	;

typeDef: "typedef" type NAME
	;

definedType: 

varDecl: type NAME
	;

Types created in symbol table in place of NAME by first pass:
	structType - result of a structDef
	definedType - result of a typeDef
	
