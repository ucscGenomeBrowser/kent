# taxonDivision.sql was originally generated by the autoSql program, which also 
# generated taxonDivision.c and taxonDivision.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#ncbi taxonomy high level divisions
CREATE TABLE taxonDivision (
    id int unsigned not null,	# division id in GenBank taxonomy database
    code char(3) not null,	# division code 3 characters 
    name varchar(255) not null,	# division name e.g. BCT, PLN, VRT, MAM, PRI...
    comments varchar(255) not null,	# free-text comments 
              #Indices
    PRIMARY KEY(id)
);
