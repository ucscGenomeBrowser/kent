"""
Describes all of the URLs that this site supports and what Django application to call for them.
This file was originally generated by the Django framework.
"""

from django.conf.urls.defaults import patterns, include, url

# Uncomment the next two lines to enable the admin:
from django.contrib import admin
admin.autodiscover()

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'mysite.views.home', name='home'),
    # url(r'^mysite/', include('mysite.foo.urls')),

    # Uncomment the admin/doc line below to enable admin documentation:
    # url(r'^admin/doc/', include('django.contrib.admindocs.urls')),

    url(r'^admin/', include(admin.site.urls)),   # Enable the admin
    url(r'^cvDb/', include('cvDb.urls')),
    url(r'^polls/', include('polls.urls')),
    url(r'^djBook/', include('djBook.urls')),
)
