# kgColor.sql was originally generated by the autoSql program, which also 
# generated kgColor.c and kgColor.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#What color to draw known gene in?
CREATE TABLE kgColor (
    kgID varchar(255) not null,	# Known gene ID
    r tinyint unsigned not null,	# Red component 0-255
    g tinyint unsigned not null,	# Green component 0-255
    b tinyint unsigned not null,	# Blue component 0-255
              #Indices
    PRIMARY KEY(kgID)
);
