/* gencodePubMed.c was originally generated by the autoSql program, which also 
 * generated gencodePubMed.h and gencodePubMed.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodePubMed.h"



char *gencodePubMedCommaSepFieldNames = "transcriptId,pubMedId";

void gencodePubMedStaticLoad(char **row, struct gencodePubMed *ret)
/* Load a row from gencodePubMed table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->pubMedId = sqlSigned(row[1]);
}

struct gencodePubMed *gencodePubMedLoad(char **row)
/* Load a gencodePubMed from row fetched with select * from gencodePubMed
 * from database.  Dispose of this with gencodePubMedFree(). */
{
struct gencodePubMed *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->pubMedId = sqlSigned(row[1]);
return ret;
}

struct gencodePubMed *gencodePubMedLoadAll(char *fileName) 
/* Load all gencodePubMed from a whitespace-separated file.
 * Dispose of this with gencodePubMedFreeList(). */
{
struct gencodePubMed *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileRow(lf, row))
    {
    el = gencodePubMedLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodePubMed *gencodePubMedLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodePubMed from a chopper separated file.
 * Dispose of this with gencodePubMedFreeList(). */
{
struct gencodePubMed *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodePubMedLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodePubMed *gencodePubMedCommaIn(char **pS, struct gencodePubMed *ret)
/* Create a gencodePubMed out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodePubMed */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->pubMedId = sqlSignedComma(&s);
*pS = s;
return ret;
}

void gencodePubMedFree(struct gencodePubMed **pEl)
/* Free a single dynamically allocated gencodePubMed such as created
 * with gencodePubMedLoad(). */
{
struct gencodePubMed *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freez(pEl);
}

void gencodePubMedFreeList(struct gencodePubMed **pList)
/* Free a list of dynamically allocated gencodePubMed's */
{
struct gencodePubMed *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodePubMedFree(&el);
    }
*pList = NULL;
}

void gencodePubMedOutput(struct gencodePubMed *el, FILE *f, char sep, char lastSep) 
/* Print out gencodePubMed.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->pubMedId);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

