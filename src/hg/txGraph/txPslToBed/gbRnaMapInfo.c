/* gbRnaMapInfo.c was originally generated by the autoSql program, which also 
 * generated gbRnaMapInfo.h and gbRnaMapInfo.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gbRnaMapInfo.h"

static char const rcsid[] = "$Id: gbRnaMapInfo.c,v 1.1 2007/02/17 06:27:39 kent Exp $";

void gbRnaMapInfoStaticLoad(char **row, struct gbRnaMapInfo *ret)
/* Load a row from gbRnaMapInfo table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->chrom = row[0];
ret->chromStart = sqlSigned(row[1]);
ret->chromEnd = sqlSigned(row[2]);
ret->accession = row[3];
ret->genbankCds = row[4];
ret->genbankFullCds = row[5];
ret->intronNudged = row[6];
ret->directionFlipped = row[7];
ret->mergedSmallGap = row[8];
ret->splitLargeGap = row[9];
ret->splitLargGapInCds = row[10];
ret->smallGapBustsFrame = row[11];
ret->largeGapBustsFrame = row[12];
ret->intronNudgeBustsFrame = row[13];
ret->cdsOutside = row[14];
ret->outputCds = row[15];
}

struct gbRnaMapInfo *gbRnaMapInfoLoad(char **row)
/* Load a gbRnaMapInfo from row fetched with select * from gbRnaMapInfo
 * from database.  Dispose of this with gbRnaMapInfoFree(). */
{
struct gbRnaMapInfo *ret;

AllocVar(ret);
ret->chrom = cloneString(row[0]);
ret->chromStart = sqlSigned(row[1]);
ret->chromEnd = sqlSigned(row[2]);
ret->accession = cloneString(row[3]);
ret->genbankCds = cloneString(row[4]);
ret->genbankFullCds = cloneString(row[5]);
ret->intronNudged = cloneString(row[6]);
ret->directionFlipped = cloneString(row[7]);
ret->mergedSmallGap = cloneString(row[8]);
ret->splitLargeGap = cloneString(row[9]);
ret->splitLargGapInCds = cloneString(row[10]);
ret->smallGapBustsFrame = cloneString(row[11]);
ret->largeGapBustsFrame = cloneString(row[12]);
ret->intronNudgeBustsFrame = cloneString(row[13]);
ret->cdsOutside = cloneString(row[14]);
ret->outputCds = cloneString(row[15]);
return ret;
}

struct gbRnaMapInfo *gbRnaMapInfoLoadAll(char *fileName) 
/* Load all gbRnaMapInfo from a whitespace-separated file.
 * Dispose of this with gbRnaMapInfoFreeList(). */
{
struct gbRnaMapInfo *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[16];

while (lineFileRow(lf, row))
    {
    el = gbRnaMapInfoLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gbRnaMapInfo *gbRnaMapInfoLoadAllByChar(char *fileName, char chopper) 
/* Load all gbRnaMapInfo from a chopper separated file.
 * Dispose of this with gbRnaMapInfoFreeList(). */
{
struct gbRnaMapInfo *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[16];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gbRnaMapInfoLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gbRnaMapInfo *gbRnaMapInfoCommaIn(char **pS, struct gbRnaMapInfo *ret)
/* Create a gbRnaMapInfo out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gbRnaMapInfo */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->chrom = sqlStringComma(&s);
ret->chromStart = sqlSignedComma(&s);
ret->chromEnd = sqlSignedComma(&s);
ret->accession = sqlStringComma(&s);
ret->genbankCds = sqlStringComma(&s);
ret->genbankFullCds = sqlStringComma(&s);
ret->intronNudged = sqlStringComma(&s);
ret->directionFlipped = sqlStringComma(&s);
ret->mergedSmallGap = sqlStringComma(&s);
ret->splitLargeGap = sqlStringComma(&s);
ret->splitLargGapInCds = sqlStringComma(&s);
ret->smallGapBustsFrame = sqlStringComma(&s);
ret->largeGapBustsFrame = sqlStringComma(&s);
ret->intronNudgeBustsFrame = sqlStringComma(&s);
ret->cdsOutside = sqlStringComma(&s);
ret->outputCds = sqlStringComma(&s);
*pS = s;
return ret;
}

void gbRnaMapInfoFree(struct gbRnaMapInfo **pEl)
/* Free a single dynamically allocated gbRnaMapInfo such as created
 * with gbRnaMapInfoLoad(). */
{
struct gbRnaMapInfo *el;

if ((el = *pEl) == NULL) return;
freeMem(el->chrom);
freeMem(el->accession);
freeMem(el->genbankCds);
freeMem(el->genbankFullCds);
freeMem(el->intronNudged);
freeMem(el->directionFlipped);
freeMem(el->mergedSmallGap);
freeMem(el->splitLargeGap);
freeMem(el->splitLargGapInCds);
freeMem(el->smallGapBustsFrame);
freeMem(el->largeGapBustsFrame);
freeMem(el->intronNudgeBustsFrame);
freeMem(el->cdsOutside);
freeMem(el->outputCds);
freez(pEl);
}

void gbRnaMapInfoFreeList(struct gbRnaMapInfo **pList)
/* Free a list of dynamically allocated gbRnaMapInfo's */
{
struct gbRnaMapInfo *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gbRnaMapInfoFree(&el);
    }
*pList = NULL;
}

void gbRnaMapInfoOutput(struct gbRnaMapInfo *el, FILE *f, char sep, char lastSep) 
/* Print out gbRnaMapInfo.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->chrom);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->chromStart);
fputc(sep,f);
fprintf(f, "%d", el->chromEnd);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->accession);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->genbankCds);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->genbankFullCds);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->intronNudged);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->directionFlipped);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->mergedSmallGap);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->splitLargeGap);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->splitLargGapInCds);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->smallGapBustsFrame);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->largeGapBustsFrame);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->intronNudgeBustsFrame);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->cdsOutside);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->outputCds);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

