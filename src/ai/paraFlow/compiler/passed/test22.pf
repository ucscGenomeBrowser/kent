to freen(float x) into (float square, int mod4)
    {
    int i = x;
    square = x*x;
    mod4 = i&3;
    }

to square(float a) into float result
   {
   result = a*a;
   }

class point
   {
   int x,y;
   }

point pt = freen(10);

float x,y;

(x, y) = freen(10);
