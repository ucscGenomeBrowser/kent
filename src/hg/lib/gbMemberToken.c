/* gbMemberToken.c was originally generated by the autoSql program, which also 
 * generated gbMemberToken.h and gbMemberToken.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gbMemberToken.h"



char *gbMemberTokenCommaSepFieldNames = "token,userName,createTime,validUntil";

void gbMemberTokenStaticLoad(char **row, struct gbMemberToken *ret)
/* Load a row from gbMemberToken table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->token = sqlUnsigned(row[0]);
ret->userName = row[1];
ret->createTime = row[2];
ret->validUntil = row[3];
}

struct gbMemberToken *gbMemberTokenLoad(char **row)
/* Load a gbMemberToken from row fetched with select * from gbMemberToken
 * from database.  Dispose of this with gbMemberTokenFree(). */
{
struct gbMemberToken *ret;

AllocVar(ret);
ret->token = sqlUnsigned(row[0]);
ret->userName = cloneString(row[1]);
ret->createTime = cloneString(row[2]);
ret->validUntil = cloneString(row[3]);
return ret;
}

struct gbMemberToken *gbMemberTokenLoadAll(char *fileName) 
/* Load all gbMemberToken from a whitespace-separated file.
 * Dispose of this with gbMemberTokenFreeList(). */
{
struct gbMemberToken *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileRow(lf, row))
    {
    el = gbMemberTokenLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gbMemberToken *gbMemberTokenLoadAllByChar(char *fileName, char chopper) 
/* Load all gbMemberToken from a chopper separated file.
 * Dispose of this with gbMemberTokenFreeList(). */
{
struct gbMemberToken *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gbMemberTokenLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gbMemberToken *gbMemberTokenCommaIn(char **pS, struct gbMemberToken *ret)
/* Create a gbMemberToken out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gbMemberToken */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->token = sqlUnsignedComma(&s);
ret->userName = sqlStringComma(&s);
ret->createTime = sqlStringComma(&s);
ret->validUntil = sqlStringComma(&s);
*pS = s;
return ret;
}

void gbMemberTokenFree(struct gbMemberToken **pEl)
/* Free a single dynamically allocated gbMemberToken such as created
 * with gbMemberTokenLoad(). */
{
struct gbMemberToken *el;

if ((el = *pEl) == NULL) return;
freeMem(el->userName);
freeMem(el->createTime);
freeMem(el->validUntil);
freez(pEl);
}

void gbMemberTokenFreeList(struct gbMemberToken **pList)
/* Free a list of dynamically allocated gbMemberToken's */
{
struct gbMemberToken *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gbMemberTokenFree(&el);
    }
*pList = NULL;
}

void gbMemberTokenOutput(struct gbMemberToken *el, FILE *f, char sep, char lastSep) 
/* Print out gbMemberToken.  Separate fields with sep. Follow last field with lastSep. */
{
fprintf(f, "%u", el->token);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->userName);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->createTime);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->validUntil);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

