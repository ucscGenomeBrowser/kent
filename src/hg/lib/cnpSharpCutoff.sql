# cnpSharpCutoff.sql was originally generated by the autoSql program, which also 
# generated cnpSharpCutoff.c and cnpSharpCutoff.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#CNP cutoff data from Sharp lab
CREATE TABLE cnpSharpCutoff (
    sample varchar(255) not null,	# Sample name
    batch int unsigned not null,	# Batch number
    value float not null,	# Data value
              #Indices
    PRIMARY KEY(sample)
);
