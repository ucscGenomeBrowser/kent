table polyASize
"used to read output of faPolyASizes"
    (
    string id;            "sequence id"
    uint seqSize;         "sequence size"
    uint tailPolyASize;   "size of poly-A at head (some noise allowed)"
    uint headPolyTSize;   "size of poly-T at tail (some noise allowed)"
    )
