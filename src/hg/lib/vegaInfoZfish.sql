# vegaInfoZfish.sql was originally generated by the autoSql program, which also 
# generated vegaInfoZfish.c and vegaInfoZfish.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Vega Genes track additional information
CREATE TABLE vegaInfoZfish (
    transcriptId varchar(255) not null,	# Vega transcript ID
    geneId varchar(255) not null,	# Vega gene ID (OTTER ID)
    sangerName varchar(255) not null,	# Sanger gene name
    zfinId varchar(255) not null,	# ZFIN ID
    zfinSymbol varchar(255) not null,	# ZFIN gene symbol
    method varchar(255) not null,	# GTF method field
    geneDesc longblob not null,	# Vega gene description
    confidence varchar(255) not null,	# Status (KNOWN, NOVEL, PUTATIVE, PREDICTED)
              #Indices
    PRIMARY KEY(transcriptId),
    INDEX(geneId)
);
