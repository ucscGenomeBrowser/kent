#!/bin/bash -e

# create tableList show tables cache for all assemblies (redmine #3780)

for db in `hgsql information_schema -e "show databases" | tail -n +2` ; do
    if [ "$db" != "information_schema" ]
        then
        ./makeTableList $db
    fi
done
