# mouseSynWhd.sql was originally generated by the autoSql program, which also 
# generated mouseSynWhd.c and mouseSynWhd.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Whitehead Synteny between mouse and human chromosomes (bed 6 +).
CREATE TABLE mouseSynWhd (
    chrom varchar(255) not null,	# Human chromosome
    chromStart int unsigned not null,	# Start position in human chromosome
    chromEnd int unsigned not null,	# End position in human chromosome
    name varchar(255) not null,	# Name of mouse chromosome
    score int unsigned not null,	# Unused (bed 6 compatibility)
    strand char(1) not null,	# + or -
    mouseStart int unsigned not null,	# Start position in mouse chromosome
    mouseEnd int unsigned not null,	# End position in mouse chromosome
    segLabel varchar(255) not null	# Whitehead segment label
);
