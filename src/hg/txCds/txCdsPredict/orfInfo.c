/* orfInfo.c was originally generated by the autoSql program, which also 
 * generated orfInfo.h and orfInfo.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "orfInfo.h"

static char const rcsid[] = "$Id: orfInfo.c,v 1.1 2007/03/15 07:42:43 kent Exp $";

void orfInfoStaticLoad(char **row, struct orfInfo *ret)
/* Load a row from orfInfo table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->rnaName = row[0];
ret->rnaSize = sqlSigned(row[1]);
ret->start = sqlSigned(row[2]);
ret->end = sqlSigned(row[3]);
ret->score = sqlDouble(row[4]);
}

struct orfInfo *orfInfoLoad(char **row)
/* Load a orfInfo from row fetched with select * from orfInfo
 * from database.  Dispose of this with orfInfoFree(). */
{
struct orfInfo *ret;

AllocVar(ret);
ret->rnaName = cloneString(row[0]);
ret->rnaSize = sqlSigned(row[1]);
ret->start = sqlSigned(row[2]);
ret->end = sqlSigned(row[3]);
ret->score = sqlDouble(row[4]);
return ret;
}

struct orfInfo *orfInfoLoadAll(char *fileName) 
/* Load all orfInfo from a whitespace-separated file.
 * Dispose of this with orfInfoFreeList(). */
{
struct orfInfo *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[5];

while (lineFileRow(lf, row))
    {
    el = orfInfoLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct orfInfo *orfInfoLoadAllByChar(char *fileName, char chopper) 
/* Load all orfInfo from a chopper separated file.
 * Dispose of this with orfInfoFreeList(). */
{
struct orfInfo *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[5];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = orfInfoLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct orfInfo *orfInfoCommaIn(char **pS, struct orfInfo *ret)
/* Create a orfInfo out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new orfInfo */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->rnaName = sqlStringComma(&s);
ret->rnaSize = sqlSignedComma(&s);
ret->start = sqlSignedComma(&s);
ret->end = sqlSignedComma(&s);
ret->score = sqlDoubleComma(&s);
*pS = s;
return ret;
}

void orfInfoFree(struct orfInfo **pEl)
/* Free a single dynamically allocated orfInfo such as created
 * with orfInfoLoad(). */
{
struct orfInfo *el;

if ((el = *pEl) == NULL) return;
freeMem(el->rnaName);
freez(pEl);
}

void orfInfoFreeList(struct orfInfo **pList)
/* Free a list of dynamically allocated orfInfo's */
{
struct orfInfo *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    orfInfoFree(&el);
    }
*pList = NULL;
}

void orfInfoOutput(struct orfInfo *el, FILE *f, char sep, char lastSep) 
/* Print out orfInfo.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->rnaName);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->rnaSize);
fputc(sep,f);
fprintf(f, "%d", el->start);
fputc(sep,f);
fprintf(f, "%d", el->end);
fputc(sep,f);
fprintf(f, "%g", el->score);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

