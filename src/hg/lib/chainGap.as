table chainGap
"alignment block in chain"
    (
    uint tStart; "Alignment start position in target"
    uint tEnd; "Alignment end position in target"
    uint qGap; "Gap size in query"
    uint chainId; "chain id in chain table"
    )
