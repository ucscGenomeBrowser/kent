# Fly-specific link info.
# This contains info to construct the quick links. 

name genome
tables sgdGene 
idSql select chrom,txStart+1,txEnd from sgdGene where name = '%s'

name family
tables sgdGene
idSql select name from sgdGene where name = '%s'

name sgd
shortLabel SGD
tables sgdGene
idSql select name from sgdGene where name = '%s'
url http://db.yeastgenome.org/cgi-bin/SGD/locus.pl?locus=%s
priority 25

name swissProt
tables sgdGene
idSql select proteinID from sgdGene where name = '%s'

name pubMed
shortLabel PubMed
tables bdgpSwissProt
idSql select value from sgdToName where name = '%s'
url http://www.ncbi.nlm.nih.gov/entrez/query.fcgi?cmd=Search&db=PubMed&term=%s&doptcmdl=DocSum&tool=genome.ucsc.edu
priority 60

name geneCards
hide

name stanfordSource
hide

name cgap
hide

name ensembl
hide

name aceView
hide
