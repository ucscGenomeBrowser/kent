# Local declaration so that local gold.html is picked up.
track gold override
visibility dense
html gold

# Local declaration so that local gap.html is picked up.
track gap override
visibility hide
html gap

track mrna override
visibility hide

track intronEst override
visibility hide

track est override
visibility dense

track xenoMrna override
visibility dense

track gc5BaseBw override
visibility full

track ws210Gene override
shortLabel WS210 Genes
longLabel Genes predictions from WormBase WS210 release lifted to caeJap3

track windowmaskerSdust override
visibility dense

track chainNetCe9 override
otherDb ce9
visibility dense
