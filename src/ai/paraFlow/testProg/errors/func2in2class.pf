
to incSquare(float a, b) into float a1,bb
   {
   a1 = a + 1;
   bb = b * b;
   }
  
float a,b;

class pt
   {
   float x,y;
   }

// (a,b) = incSquare(2,2);
// print("inc " + a + ", square " + b);
// (a,b) = incSquare(incSquare(2,2));
pt quad = (4,4);
print(quad);
pt pt = incSquare(3,3);
print(pt);
