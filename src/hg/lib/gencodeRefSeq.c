/* gencodeRefSeq.c was originally generated by the autoSql program, which also 
 * generated gencodeRefSeq.h and gencodeRefSeq.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodeRefSeq.h"



char *gencodeRefSeqCommaSepFieldNames = "transcriptId,rnaAcc,pepAcc";

void gencodeRefSeqStaticLoad(char **row, struct gencodeRefSeq *ret)
/* Load a row from gencodeRefSeq table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->rnaAcc = row[1];
ret->pepAcc = row[2];
}

struct gencodeRefSeq *gencodeRefSeqLoad(char **row)
/* Load a gencodeRefSeq from row fetched with select * from gencodeRefSeq
 * from database.  Dispose of this with gencodeRefSeqFree(). */
{
struct gencodeRefSeq *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->rnaAcc = cloneString(row[1]);
ret->pepAcc = cloneString(row[2]);
return ret;
}

struct gencodeRefSeq *gencodeRefSeqLoadAll(char *fileName) 
/* Load all gencodeRefSeq from a whitespace-separated file.
 * Dispose of this with gencodeRefSeqFreeList(). */
{
struct gencodeRefSeq *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileRow(lf, row))
    {
    el = gencodeRefSeqLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeRefSeq *gencodeRefSeqLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodeRefSeq from a chopper separated file.
 * Dispose of this with gencodeRefSeqFreeList(). */
{
struct gencodeRefSeq *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodeRefSeqLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeRefSeq *gencodeRefSeqCommaIn(char **pS, struct gencodeRefSeq *ret)
/* Create a gencodeRefSeq out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodeRefSeq */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->rnaAcc = sqlStringComma(&s);
ret->pepAcc = sqlStringComma(&s);
*pS = s;
return ret;
}

void gencodeRefSeqFree(struct gencodeRefSeq **pEl)
/* Free a single dynamically allocated gencodeRefSeq such as created
 * with gencodeRefSeqLoad(). */
{
struct gencodeRefSeq *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freeMem(el->rnaAcc);
freeMem(el->pepAcc);
freez(pEl);
}

void gencodeRefSeqFreeList(struct gencodeRefSeq **pList)
/* Free a list of dynamically allocated gencodeRefSeq's */
{
struct gencodeRefSeq *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodeRefSeqFree(&el);
    }
*pList = NULL;
}

void gencodeRefSeqOutput(struct gencodeRefSeq *el, FILE *f, char sep, char lastSep) 
/* Print out gencodeRefSeq.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->rnaAcc);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->pepAcc);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

