include ../../inc/common.mk

install:
	cp -p updateTimes.csh         ${SCRIPTS}
	cp -p compareTrackDbs.csh     ${SCRIPTS}
	cp -p compareHgFindSpecs.csh  ${SCRIPTS}
	cp -p checkStaticLinks.csh    ${SCRIPTS}
	cp -p checkAllStaticLinks.csh ${SCRIPTS}
	cp -p runJoiner.csh           ${SCRIPTS}

