/* wgEncodeGencodeSource.c was originally generated by the autoSql program, which also 
 * generated wgEncodeGencodeSource.h and wgEncodeGencodeSource.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "wgEncodeGencodeSource.h"

static char const rcsid[] = "$Id:$";

/* definitions for source column */
static char *values_source[] = {"ensembl", "shares_CDS", "shares_CDS_and_UTR", NULL};
static struct hash *valhash_source = NULL;

void wgEncodeGencodeSourceStaticLoad(char **row, struct wgEncodeGencodeSource *ret)
/* Load a row from wgEncodeGencodeSource table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->source = sqlEnumParse(row[1], values_source, &valhash_source);
}

struct wgEncodeGencodeSource *wgEncodeGencodeSourceLoad(char **row)
/* Load a wgEncodeGencodeSource from row fetched with select * from wgEncodeGencodeSource
 * from database.  Dispose of this with wgEncodeGencodeSourceFree(). */
{
struct wgEncodeGencodeSource *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->source = sqlEnumParse(row[1], values_source, &valhash_source);
return ret;
}

struct wgEncodeGencodeSource *wgEncodeGencodeSourceLoadAll(char *fileName) 
/* Load all wgEncodeGencodeSource from a whitespace-separated file.
 * Dispose of this with wgEncodeGencodeSourceFreeList(). */
{
struct wgEncodeGencodeSource *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileRow(lf, row))
    {
    el = wgEncodeGencodeSourceLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct wgEncodeGencodeSource *wgEncodeGencodeSourceLoadAllByChar(char *fileName, char chopper) 
/* Load all wgEncodeGencodeSource from a chopper separated file.
 * Dispose of this with wgEncodeGencodeSourceFreeList(). */
{
struct wgEncodeGencodeSource *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = wgEncodeGencodeSourceLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct wgEncodeGencodeSource *wgEncodeGencodeSourceCommaIn(char **pS, struct wgEncodeGencodeSource *ret)
/* Create a wgEncodeGencodeSource out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new wgEncodeGencodeSource */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->source = sqlEnumComma(&s, values_source, &valhash_source);
*pS = s;
return ret;
}

void wgEncodeGencodeSourceFree(struct wgEncodeGencodeSource **pEl)
/* Free a single dynamically allocated wgEncodeGencodeSource such as created
 * with wgEncodeGencodeSourceLoad(). */
{
struct wgEncodeGencodeSource *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freez(pEl);
}

void wgEncodeGencodeSourceFreeList(struct wgEncodeGencodeSource **pList)
/* Free a list of dynamically allocated wgEncodeGencodeSource's */
{
struct wgEncodeGencodeSource *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    wgEncodeGencodeSourceFree(&el);
    }
*pList = NULL;
}

void wgEncodeGencodeSourceOutput(struct wgEncodeGencodeSource *el, FILE *f, char sep, char lastSep) 
/* Print out wgEncodeGencodeSource.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
sqlEnumPrint(f, el->source, values_source);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

