# affyAllExonProbe.sql was originally generated by the autoSql program, which also 
# generated affyAllExonProbe.c and affyAllExonProbe.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Information for a probe on an Affymetrix all-exon chip
CREATE TABLE affyAllExonProbe (
    name varchar(255) not null,	# ID of the probe
    numIndependentProbes int unsigned not null,	# ??? Totatlly unsure
    exonClusterId varchar(255) not null,	# ??? ID of the probes exon cluster
    numNonOverlapProbes int unsigned not null,	# ??? Probably related to numIndependentProbes
    probeCount int unsigned not null,	# ??? These are starting to all sound the same
    transcriptClustId varchar(255) not null,	# Transcript cluster this exon probe belongs to
    probesetType int unsigned not null,	# ???
    numXHybeProbe int unsigned not null,	# ??? cross hyb something
    psrId int unsigned not null,	# ???
    level varchar(255) not null,	# ???
    evidence varchar(255) not null,	# Evidence for the exon from various sources
    bounded int unsigned not null,	# ??? Yes or No
    cds int unsigned not null,	# ??? Yes or No
              #Indices
    INDEX(name(7))
);
