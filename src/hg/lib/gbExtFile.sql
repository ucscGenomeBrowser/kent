# gbExtFile.sql was originally generated by the autoSql program, which also 
# generated gbExtFile.c and gbExtFile.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#gbExtFile
CREATE TABLE gbExtFile (
    id int unsigned not null,	# id
    path varchar(255) not null,	# path
    size int unsigned not null,	# size
              #Indices
    PRIMARY KEY(id)
);
