# geneCheckDetails.sql was originally generated by the autoSql program, which also 
# generated geneCheckDetails.c and geneCheckDetails.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#details from gene-check program
CREATE TABLE geneCheckDetails (
    acc varchar(255) not null,	# gene id or accession
    problem varchar(255) not null,	# problem
    info varchar(255) not null,	# additional information 
    chr varchar(255) not null,	# chromosome
    chrStart int unsigned not null,	# chromosome start
    chrEnd int unsigned not null,	# chromosome end
    #Indices
    index(acc)
);
