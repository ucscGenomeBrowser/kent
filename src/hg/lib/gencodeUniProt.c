/* gencodeUniProt.c was originally generated by the autoSql program, which also 
 * generated gencodeUniProt.h and gencodeUniProt.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodeUniProt.h"



char *gencodeUniProtCommaSepFieldNames = "transcriptId,acc,name,dataset";

/* definitions for dataset column */
static char *values_dataset[] = {"SwissProt", "TrEMBL", NULL};
static struct hash *valhash_dataset = NULL;

void gencodeUniProtStaticLoad(char **row, struct gencodeUniProt *ret)
/* Load a row from gencodeUniProt table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->acc = row[1];
ret->name = row[2];
ret->dataset = sqlEnumParse(row[3], values_dataset, &valhash_dataset);
}

struct gencodeUniProt *gencodeUniProtLoad(char **row)
/* Load a gencodeUniProt from row fetched with select * from gencodeUniProt
 * from database.  Dispose of this with gencodeUniProtFree(). */
{
struct gencodeUniProt *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->acc = cloneString(row[1]);
ret->name = cloneString(row[2]);
ret->dataset = sqlEnumParse(row[3], values_dataset, &valhash_dataset);
return ret;
}

struct gencodeUniProt *gencodeUniProtLoadAll(char *fileName) 
/* Load all gencodeUniProt from a whitespace-separated file.
 * Dispose of this with gencodeUniProtFreeList(). */
{
struct gencodeUniProt *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileRow(lf, row))
    {
    el = gencodeUniProtLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeUniProt *gencodeUniProtLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodeUniProt from a chopper separated file.
 * Dispose of this with gencodeUniProtFreeList(). */
{
struct gencodeUniProt *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodeUniProtLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodeUniProt *gencodeUniProtCommaIn(char **pS, struct gencodeUniProt *ret)
/* Create a gencodeUniProt out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodeUniProt */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->acc = sqlStringComma(&s);
ret->name = sqlStringComma(&s);
ret->dataset = sqlEnumComma(&s, values_dataset, &valhash_dataset);
*pS = s;
return ret;
}

void gencodeUniProtFree(struct gencodeUniProt **pEl)
/* Free a single dynamically allocated gencodeUniProt such as created
 * with gencodeUniProtLoad(). */
{
struct gencodeUniProt *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freeMem(el->acc);
freeMem(el->name);
freez(pEl);
}

void gencodeUniProtFreeList(struct gencodeUniProt **pList)
/* Free a list of dynamically allocated gencodeUniProt's */
{
struct gencodeUniProt *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodeUniProtFree(&el);
    }
*pList = NULL;
}

void gencodeUniProtOutput(struct gencodeUniProt *el, FILE *f, char sep, char lastSep) 
/* Print out gencodeUniProt.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->acc);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->name);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
sqlEnumPrint(f, el->dataset, values_dataset);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

