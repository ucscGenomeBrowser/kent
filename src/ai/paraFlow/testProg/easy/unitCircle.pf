class math
    {
    flow cos(double x) into double y {y=0;}
    flow sin(double x) into double y {y=1;}
    }


flow unitCircle(float radians) into (float x, float y)
    {
    math math;
    x = math.cos(radians);
    y = math.sin(radians);
    }
