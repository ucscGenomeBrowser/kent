class math
    {
    to cos(double x) into double y {y=0;}
    to sin(double x) into double y {y=1;}
    }

math math;

flow unitCircle(float radians) into (float x, float y)
    {
    x = math.cos(radians);
    y = math.sin(radians);
    }
