# dv.sql was originally generated by the autoSql program, which also 
# generated dv.c and dv.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#disease variation
CREATE TABLE dv (
    spID varchar(40) not null,	# Swiss-prot ID
    start int unsigned not null,	# Start position
    len int unsigned not null,	# Length
    orig varchar(255) not null,	# Original
    variant varchar(255) not null,	# Variant
    varId varchar(40) not null,	# Variant identifier
    varStatus varchar(10) not null,	# Variant status (always unknown)
              #Indices
    KEY spID (spID(16)),
    KEY varId (varID(16))
);
