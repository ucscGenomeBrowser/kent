kentSrc = ../../../..
include ../../../../inc/common.mk

# These mixin files are Javascript not JSX, so use simple.mk not ../react.mk

JS_DIR=../..
JS_FILES= ImmutableUpdate.js PathUpdate.js PathUpdateOptional.js

include ${JS_DIR}/install.mk
include ${JS_DIR}/simple.mk
