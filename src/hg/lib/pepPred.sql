# pepPred.sql was originally generated by the autoSql program, which also 
# generated pepPred.c and pepPred.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#A predicted peptide - linked to a predicted gene.
CREATE TABLE pepPred (
    name varchar(255) not null,	# Name of gene - same as in genePred
    seq longblob not null,	# Peptide sequence
              #Indices
    PRIMARY KEY(name)
);
