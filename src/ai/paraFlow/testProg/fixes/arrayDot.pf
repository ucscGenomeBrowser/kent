class pair
   {
   int a,b;
   }

array of pair pairs = ((10,20),(30,40),(50,60));
print(pairs);
for (int i=0; i<pairs.size; ++i)
    {
    if (i != 0)
	prin(" | ");
    prin(pairs[i].a + " " + pairs[i].b);
    }
print("");

