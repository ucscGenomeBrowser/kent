#This file contains default definitions of genome-specific
#variables.  It must start with 'name global'
#
name global
examples Some examples of search terms are 'FOXA1' 'HOXA9' and 'MAP kinase.'
geneTable knownGene
cannonicalTable knownCannonical
allTranscriptQuery select name,chrom,txStart,txEnd,proteinId from knownGene
allGeneQuery select transcript,chrom,chromStart,chromEnd,protein from knownCannonical
