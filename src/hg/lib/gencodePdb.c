/* gencodePdb.c was originally generated by the autoSql program, which also 
 * generated gencodePdb.h and gencodePdb.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "gencodePdb.h"



char *gencodePdbCommaSepFieldNames = "transcriptId,pdbId";

void gencodePdbStaticLoad(char **row, struct gencodePdb *ret)
/* Load a row from gencodePdb table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->transcriptId = row[0];
ret->pdbId = row[1];
}

struct gencodePdb *gencodePdbLoad(char **row)
/* Load a gencodePdb from row fetched with select * from gencodePdb
 * from database.  Dispose of this with gencodePdbFree(). */
{
struct gencodePdb *ret;

AllocVar(ret);
ret->transcriptId = cloneString(row[0]);
ret->pdbId = cloneString(row[1]);
return ret;
}

struct gencodePdb *gencodePdbLoadAll(char *fileName) 
/* Load all gencodePdb from a whitespace-separated file.
 * Dispose of this with gencodePdbFreeList(). */
{
struct gencodePdb *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileRow(lf, row))
    {
    el = gencodePdbLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodePdb *gencodePdbLoadAllByChar(char *fileName, char chopper) 
/* Load all gencodePdb from a chopper separated file.
 * Dispose of this with gencodePdbFreeList(). */
{
struct gencodePdb *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[2];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = gencodePdbLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct gencodePdb *gencodePdbCommaIn(char **pS, struct gencodePdb *ret)
/* Create a gencodePdb out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new gencodePdb */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->transcriptId = sqlStringComma(&s);
ret->pdbId = sqlStringComma(&s);
*pS = s;
return ret;
}

void gencodePdbFree(struct gencodePdb **pEl)
/* Free a single dynamically allocated gencodePdb such as created
 * with gencodePdbLoad(). */
{
struct gencodePdb *el;

if ((el = *pEl) == NULL) return;
freeMem(el->transcriptId);
freeMem(el->pdbId);
freez(pEl);
}

void gencodePdbFreeList(struct gencodePdb **pList)
/* Free a list of dynamically allocated gencodePdb's */
{
struct gencodePdb *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    gencodePdbFree(&el);
    }
*pList = NULL;
}

void gencodePdbOutput(struct gencodePdb *el, FILE *f, char sep, char lastSep) 
/* Print out gencodePdb.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->transcriptId);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->pdbId);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

