# CREATING DATABASE
# First go into mysql and at prompt do
#     create database genePix
# and then quit.  Then:
cd ~/kent/src/hg/genePix
hgsql genePix < genePix.sql

# LOADING PAUL GRAY/MAHONEY MOUSE IN SITUS
# First get data from Paul Gray <pgray@mac.com>
# in the form of a couple of directories full of
# images and some Excel spreadsheets.  Turn spreadsheet
# into tab-separated file.  Create new directories 
# /gbdb/genePix/full/inSitu/Mouse/mahoney
# and put stuff in there.  Whole mounts go into wholeMount
# subdirectory and others into slices subdirectory.
# Normalize slices with Image Magick
#     convert in.jpg -normalize out.jpg
# Create thumbnails with
#     convert -size 200x200 in.jpg -resize 200x200 out.jpg
# 
# Next to help clean up data do PCR of primers in cloning.tab
# against all mouse genbank/refSeq mRNA.
ssh hgwdev
cd ~/kent/src/hg/genePix/loadMahoney
hgSpeciesRna mm5 Mus musculus /panasas/store/kent/mm5Rna.fa
awk -F '\t' '{if (NR != 1 && $16 != "" && $17 != "") printf("MTF%d\t%s\t%s\n", $2,$16,$17);}' cloning.tab > cloning.pcr
# edit cloning.pcr to deal with line that says 'same as above'
isPcr -out=bed /panasas/store/kent/mm5Rna.fa cloning.pcr clonePcr.bed
loadMahoney /gbdb/genePix mm5 cloning.tab clonePcr.bed out
cd out
genePixLoad whole.ra whole.tab
genePixLoad slices.ra slices.tab

