class point
   {
   int x,y;
   }

to makePoint(float radius) into float x,y
/* Make point of given radius */
{
x = radius;
y = radius;
}

to pointFactory(int x, y) into point pt
{
pt = (x,y);
}

point pt2 = makePoint(3);
point pt3 = pointFactory(1,2);

