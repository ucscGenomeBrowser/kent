# rnaPLFold.sql was originally generated by the autoSql program, which also 
# generated rnaPLFold.c and rnaPLFold.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#RNA PL Fold -- RNA Partition Function Local Fold
CREATE TABLE rnaPLFold (
    chrom varchar(255) not null,	# Reference sequence chromosome or scaffold
    chromStart int unsigned not null,	# chromStart for reference marker
    chromEnd int unsigned not null,	# chromEnd for last marker in list
    span int unsigned not null,	# Number of positions covered by colorIndex values
    colorIndex text not null	# PLFold values encoded as indexes in a color array
);
