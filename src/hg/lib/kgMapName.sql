# kgMapName.sql was originally generated by the autoSql program, which also 
# generated kgMapName.c and kgMapName.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Maps kg pep names to common name
CREATE TABLE kgMapName (
    kgPepId varchar(255) not null,	# pep ID
    geneName varchar(255) not null,	# gene name
              #Indices
    PRIMARY KEY(kgPepId)
);
