# userDb.sql was originally generated by the autoSql program, which also 
# generated userDb.c and userDb.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#User Database
CREATE TABLE userDb (
    id int(10) unsigned not null auto_increment,	# auto-increment item ID
    contents longblob not null,	# encoded variables */
    reserved tinyint(4) not null,	# currently always zero
    firstUse datetime not null,	# first time this was used
    lastUse datetime not null,	# last time this was used
    useCount int(11) unsigned not null,	# number of times used
    sessionKey varchar(255) not null,	# random key for session security
              #Indices
    PRIMARY KEY(id)
) ENGINE=MyISAM DEFAULT CHARSET=latin1 MAX_ROWS=10000000 AVG_ROW_LENGTH=8655 PACK_KEYS=1;
