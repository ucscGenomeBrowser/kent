# Local declaration so that local gold.html is picked up.
track gold override
html gold
visibility pack

track mrna override
visibility hide

track intronEst override
visibility hide

track est override
visibility dense

track xenoMrna override
visibility dense

track windowmaskerSdust override
visibility dense

track rmsk override
visibility hide

track gc5BaseBw override
visibility full

# Local declaration so that local gap.html is picked up.
track gap override
html gap
visibility hide

track ws210Gene override
shortLabel WS210 Genes
longLabel Genes predictions from WormBase priPac1/WS210 release lifted to priPac2

track chainNetCe9 override
otherDb ce9
visibility dense

track chainNetCb3 override
otherDb cb3
visibility dense
