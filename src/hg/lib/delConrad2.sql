# delConrad2.sql was originally generated by the autoSql program, which also 
# generated delConrad2.c and delConrad2.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Deletions from Conrad
CREATE TABLE delConrad2 (
    bin smallint unsigned not null,   	# Bin for fast index
    chrom varchar(255) not null,	# Reference sequence chromosome or scaffold
    chromStart int unsigned not null,	# Start position in chrom
    chromEnd int unsigned not null,	# End position in chrom
    name varchar(255) not null,		# Name
    score int unsigned not null,	# Always 1000
    strand char(1) not null,		# Always positive
    thickStart int unsigned not null,	# Max start (can be different from chromStart)
    thickEnd int unsigned not null,	# Max end (can be different from chromEnd)
    count1 int unsigned not null,	# Count1
    count2 int unsigned not null,	# Count2
    offspring varchar(255) not null,	# HapMap identifier
    population varchar(255) not null	# HapMap population
);
