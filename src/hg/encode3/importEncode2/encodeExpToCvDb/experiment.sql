CREATE TABLE `cvDb_experiment` (
    `id` integer AUTO_INCREMENT NOT NULL PRIMARY KEY,
    `updateTime` varchar(40) NOT NULL,
    `series` varchar(50) NOT NULL,
    `accession` varchar(16) NOT NULL UNIQUE,
    `organism` integer NOT NULL,
    `lab` integer NOT NULL,
    `dataType` integer NOT NULL,
    `cellType` integer NOT NULL,
    `age` integer NOT NULL,
    `antibody` integer NOT NULL,
    `attic` integer NOT NULL,
    `category` integer NOT NULL,
    `control` integer NOT NULL,
    `fragSize` integer NOT NULL,
    `grantee` integer NOT NULL,
    `insertLength` integer NOT NULL,
    `localization` integer NOT NULL,
    `mapAlgorithm` integer NOT NULL,
    `objStatus` integer NOT NULL,
    `phase` integer NOT NULL,
    `platform` integer NOT NULL,
    `promoter` integer NOT NULL,
    `protocol` integer NOT NULL,
    `readType` integer NOT NULL,
    `region` integer NOT NULL,
    `restrictionEnzyme` integer NOT NULL,
    `rnaExtract` integer NOT NULL,
    `seqPlatform` integer NOT NULL,
    `sex` integer NOT NULL,
    `strain` integer NOT NULL,
    `tissueSourceType` integer NOT NULL,
    `treatment` integer NOT NULL,
    `version` integer NOT NULL
)
;
