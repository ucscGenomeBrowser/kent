#!/bin/sh


convolve -verbose -count=4 -logs base5.logs
convolve -verbose -html -count=2 -logs base5.logs

convolve -count=1 -verbose test0.probs
convolve -count=2 -verbose test0.probs
convolve -verbose test0.probs
convolve -count=6 -verbose test0.probs

exit 0

convolve empty.test
convolve long_line.test
