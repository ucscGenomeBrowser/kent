# rnaFold.sql was originally generated by the autoSql program, which also 
# generated rnaFold.c and rnaFold.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Info about folding of RNA into secondary structure
CREATE TABLE rnaFold (
    name varchar(255) not null,	# mRNA accession
    seq longblob not null,	# mRNA sequence (U's instead of T's)
    fold longblob not null,	# Parenthesis and .'s that describe folding
    energy float not null,	# Estimated free energy of folding (negative)
              #Indices
    PRIMARY KEY(name(10))
);
