# wgEncodeGencodePolyAFeature.sql was originally generated by the autoSql program, which also 
# generated wgEncodeGencodePolyAFeature.c and wgEncodeGencodePolyAFeature.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#GENCODE PolyA feature annotation in transcript and genome
CREATE TABLE wgEncodeGencodePolyAFeature (
    transcriptId varchar(255) not null,	# GENCODE transcript identifier
    transcriptStart int not null,	# starting position in transcript:
    transcriptEnd int not null,	# ending position in transcript:
    chrom varchar(255) not null,	# chromosome
    chromStart int not null,	# start in chromosome
    endStart int not null,	# end in chromosome
    strand char(1) not null,	# strand
    feature enum("polyA_signal", "polyA_site", "pseudo_polyA")  not null,	# type of annotation
              #Indices
    index(transcriptId)
);
