/* bigNet.h was originally generated by the autoSql program, which also 
 * generated bigNet.c and bigNet.sql.  This header links the database and
 * the RAM representation of objects. */

#ifndef BIGNET_H
#define BIGNET_H

#define BIGNET_NUM_COLS 26

extern char *bigNetCommaSepFieldNames;

struct bigNet
/* bigNet - a net of pairwise alignments in bigBed format */
    {
    struct bigNet *next;  /* Next in singly linked list. */
    char *chrom;	/* Reference sequence chromosome or scaffold */
    unsigned chromStart;	/* Start position in chromosome */
    unsigned chromEnd;	/* End position in chromosome */
    char *name;	/* Name of the query sequence */
    unsigned score;	/* Score (0-1000) */
    char strand[2];	/* + or - for strand of the query sequence */
    unsigned level;	/* Depth in the net.  Odd levels are fills, even levels are gaps */
    unsigned qStart;	/* Start of alignment on query sequence */
    unsigned qEnd;	/* End of alignment on query sequence */
    unsigned chainId;	/* Id of the chain that fills this gap.  0 for a gap */
    unsigned ali;	/* Bases in gap-free alignments */
    double chainScore;	/* Score from the chain */
    char *type;	/* Syntenic type: gap, top, syn, nonSyn or inv */
    int qOver;	/* Overlap with parent gap on query side.  -1 for undefined */
    int qFar;	/* Distance from parent gap on query side.  -1 for undefined */
    int qDup;	/* Bases with two or more copies in query.  -1 for undefined */
    int tN;	/* Unsequenced bases on target.  -1 for undefined */
    int qN;	/* Unsequenced bases on query.  -1 for undefined */
    int tR;	/* RepeatMasker bases on target.  -1 for undefined */
    int qR;	/* RepeatMasker bases on query.  -1 for undefined */
    int tNewR;	/* Lineage specific repeat bases on target.  -1 for undefined */
    int qNewR;	/* Lineage specific repeat bases on query.  -1 for undefined */
    int tOldR;	/* Bases of ancient repeats on target.  -1 for undefined */
    int qOldR;	/* Bases of ancient repeats on query.  -1 for undefined */
    int tTrf;	/* Bases of tandem repeats on target.  -1 for undefined */
    int qTrf;	/* Bases of tandem repeats on query.  -1 for undefined */
    };

void bigNetStaticLoad(char **row, struct bigNet *ret);
/* Load a row from bigNet table into ret.  The contents of ret will
 * be replaced at the next call to this function. */

struct bigNet *bigNetLoad(char **row);
/* Load a bigNet from row fetched with select * from bigNet
 * from database.  Dispose of this with bigNetFree(). */

struct bigNet *bigNetLoadAll(char *fileName);
/* Load all bigNet from whitespace-separated file.
 * Dispose of this with bigNetFreeList(). */

struct bigNet *bigNetLoadAllByChar(char *fileName, char chopper);
/* Load all bigNet from chopper separated file.
 * Dispose of this with bigNetFreeList(). */

#define bigNetLoadAllByTab(a) bigNetLoadAllByChar(a, '\t');
/* Load all bigNet from tab separated file.
 * Dispose of this with bigNetFreeList(). */

struct bigNet *bigNetCommaIn(char **pS, struct bigNet *ret);
/* Create a bigNet out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new bigNet */

void bigNetFree(struct bigNet **pEl);
/* Free a single dynamically allocated bigNet such as created
 * with bigNetLoad(). */

void bigNetFreeList(struct bigNet **pList);
/* Free a list of dynamically allocated bigNet's */

void bigNetOutput(struct bigNet *el, FILE *f, char sep, char lastSep);
/* Print out bigNet.  Separate fields with sep. Follow last field with lastSep. */

#define bigNetTabOut(el,f) bigNetOutput(el,f,'\t','\n');
/* Print out bigNet as a line in a tab-separated file. */

#define bigNetCommaOut(el,f) bigNetOutput(el,f,',',',');
/* Print out bigNet as a comma separated list including final comma. */

/* -------------------------------- End autoSql Generated Code -------------------------------- */

int bigNetCmpTarget(const void *va, const void *vb);
/* Compare to sort based on target position, then by level. */

#endif /* BIGNET_H */

