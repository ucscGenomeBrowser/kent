# tfbsCons.sql was originally generated by the autoSql program, which also 
# generated tfbsCons.c and tfbsCons.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#tfbsCons Data
CREATE TABLE tfbsCons (
    bin smallint unsigned not null,   # Bin for fast index
    chrom varchar(255) not null,	# Human chromosome
    chromStart int unsigned not null,	# Start position in chromosome
    chromEnd int unsigned not null,	# End position in chromosome
    name varchar(255) not null,	# Name of item
    score int unsigned not null,	# Score from 0-1000
    strand char(1) not null,	# + or -
    species char(6) not null,	# common name, scientific name
    factor char(64) not null,	# factor 
    id char(10) not null,	# id
              #Indices
    INDEX(name(10)),
    INDEX(chrom(12),bin),
    INDEX(chrom(12),chromStart)
);
