# pbStamp.sql was originally generated by the autoSql program, which also 
# generated pbStamp.c and pbStamp.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Info needed for a Proteom Browser Stamp
CREATE TABLE pbStamp (
    stampName char(40) not null,	# Short Name of stamp
    stampTable char(40) not null,	# Database table name of the stamp (distribution) data
    stampTitle char(40) not null,	# Stamp Title to be displayed
    len int not null,	# number of x-y pairs
    xmin float not null,	# x minimum
    xmax float not null,	# x maximum
    ymin float not null,	# y minimum
    ymax float not null,	# y maximum
    stampDesc varchar(255) not null,	# Description of the stamp
              #Indices
    PRIMARY KEY(stampName)
);
