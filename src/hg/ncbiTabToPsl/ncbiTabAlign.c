/* ncbiTabAlign.c was originally generated by the autoSql program, which also 
 * generated ncbiTabAlign.h and ncbiTabAlign.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "ncbiTabAlign.h"

static char const rcsid[] = "$Id: ncbiTabAlign.c,v 1.1 2004/01/17 09:37:17 markd Exp $";

void ncbiTabAlignStaticLoad(char **row, struct ncbiTabAlign *ret)
/* Load a row from ncbiTabAlign table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{
ret->target = row[0];
ret->alnId = sqlSigned(row[1]);
ret->type = row[2];
ret->tStart = sqlSigned(row[3]);
ret->tStop = sqlSigned(row[4]);
ret->query = row[5];
strcpy(ret->unused, row[6]);
ret->qStart = sqlSigned(row[7]);
ret->qStop = sqlSigned(row[8]);
ret->perId = atof(row[9]);
ret->score = atof(row[10]);
}

struct ncbiTabAlign *ncbiTabAlignLoad(char **row)
/* Load a ncbiTabAlign from row fetched with select * from ncbiTabAlign
 * from database.  Dispose of this with ncbiTabAlignFree(). */
{
struct ncbiTabAlign *ret;

AllocVar(ret);
ret->target = cloneString(row[0]);
ret->alnId = sqlSigned(row[1]);
ret->type = cloneString(row[2]);
ret->tStart = sqlSigned(row[3]);
ret->tStop = sqlSigned(row[4]);
ret->query = cloneString(row[5]);
strcpy(ret->unused, row[6]);
ret->qStart = sqlSigned(row[7]);
ret->qStop = sqlSigned(row[8]);
ret->perId = atof(row[9]);
ret->score = atof(row[10]);
return ret;
}

struct ncbiTabAlign *ncbiTabAlignLoadAll(char *fileName) 
/* Load all ncbiTabAlign from a whitespace-separated file.
 * Dispose of this with ncbiTabAlignFreeList(). */
{
struct ncbiTabAlign *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[11];

while (lineFileRow(lf, row))
    {
    el = ncbiTabAlignLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct ncbiTabAlign *ncbiTabAlignLoadAllByChar(char *fileName, char chopper) 
/* Load all ncbiTabAlign from a chopper separated file.
 * Dispose of this with ncbiTabAlignFreeList(). */
{
struct ncbiTabAlign *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[11];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = ncbiTabAlignLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct ncbiTabAlign *ncbiTabAlignCommaIn(char **pS, struct ncbiTabAlign *ret)
/* Create a ncbiTabAlign out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new ncbiTabAlign */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->target = sqlStringComma(&s);
ret->alnId = sqlSignedComma(&s);
ret->type = sqlStringComma(&s);
ret->tStart = sqlSignedComma(&s);
ret->tStop = sqlSignedComma(&s);
ret->query = sqlStringComma(&s);
sqlFixedStringComma(&s, ret->unused, sizeof(ret->unused));
ret->qStart = sqlSignedComma(&s);
ret->qStop = sqlSignedComma(&s);
ret->perId = sqlFloatComma(&s);
ret->score = sqlFloatComma(&s);
*pS = s;
return ret;
}

void ncbiTabAlignFree(struct ncbiTabAlign **pEl)
/* Free a single dynamically allocated ncbiTabAlign such as created
 * with ncbiTabAlignLoad(). */
{
struct ncbiTabAlign *el;

if ((el = *pEl) == NULL) return;
freeMem(el->target);
freeMem(el->type);
freeMem(el->query);
freez(pEl);
}

void ncbiTabAlignFreeList(struct ncbiTabAlign **pList)
/* Free a list of dynamically allocated ncbiTabAlign's */
{
struct ncbiTabAlign *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    ncbiTabAlignFree(&el);
    }
*pList = NULL;
}

void ncbiTabAlignOutput(struct ncbiTabAlign *el, FILE *f, char sep, char lastSep) 
/* Print out ncbiTabAlign.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->target);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->alnId);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->type);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->tStart);
fputc(sep,f);
fprintf(f, "%d", el->tStop);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->query);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->unused);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%d", el->qStart);
fputc(sep,f);
fprintf(f, "%d", el->qStop);
fputc(sep,f);
fprintf(f, "%f", el->perId);
fputc(sep,f);
fprintf(f, "%f", el->score);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

