# jaxAlleleInfo.sql was originally generated by the autoSql program, which also 
# generated jaxAlleleInfo.c and jaxAlleleInfo.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Extra info for Jackson Lab allele transcripts.
CREATE TABLE jaxAlleleInfo (
    name varchar(255) not null,	# Allele transcript name.
    mgiID varchar(255) not null,	# MGI:NNNNNN ID for links.
    source varchar(255) not null,	# Allele source.
              #Indices
    INDEX(name(32))
);
