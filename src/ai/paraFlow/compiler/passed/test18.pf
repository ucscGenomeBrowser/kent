class point
   {
   string name;
   int x,y,z=3;
   to run(){}
   to centerSquared() into int res
       {
       res = x*x + y*y + z*z;
       }
   }

point pt = ("Roger", 1, 2, 3);

var ptName = pt.name;
float ptZ = pt.z;
pt.run();

pt.x = 9;
pt.z = ptZ;
int ptDistSquared = pt.centerSquared();
