/* oregannoOther.c was originally generated by the autoSql program, which also 
 * generated oregannoOther.h and oregannoOther.sql.  This module links the database and
 * the RAM representation of objects. */

/* Copyright (C) 2014 The Regents of the University of California 
 * See README in this or parent directory for licensing information. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "oregannoOther.h"

static char const rcsid[] = "$Id:$";

void oregannoOtherStaticLoad(char **row, struct oregannoOther *ret)
/* Load a row from oregannoOther table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->bin = sqlUnsigned(row[0]);
ret->chrom = row[1];
ret->chromStart = sqlUnsigned(row[2]);
ret->chromEnd = sqlUnsigned(row[3]);
ret->id = row[4];
safecpy(ret->strand, sizeof(ret->strand), row[5]);
ret->name = row[6];
}

struct oregannoOther *oregannoOtherLoad(char **row)
/* Load a oregannoOther from row fetched with select * from oregannoOther
 * from database.  Dispose of this with oregannoOtherFree(). */
{
struct oregannoOther *ret;

AllocVar(ret);
ret->bin = sqlUnsigned(row[0]);
ret->chrom = cloneString(row[1]);
ret->chromStart = sqlUnsigned(row[2]);
ret->chromEnd = sqlUnsigned(row[3]);
ret->id = cloneString(row[4]);
safecpy(ret->strand, sizeof(ret->strand), row[5]);
ret->name = cloneString(row[6]);
return ret;
}

struct oregannoOther *oregannoOtherLoadAll(char *fileName) 
/* Load all oregannoOther from a whitespace-separated file.
 * Dispose of this with oregannoOtherFreeList(). */
{
struct oregannoOther *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[7];

while (lineFileRow(lf, row))
    {
    el = oregannoOtherLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOther *oregannoOtherLoadAllByChar(char *fileName, char chopper) 
/* Load all oregannoOther from a chopper separated file.
 * Dispose of this with oregannoOtherFreeList(). */
{
struct oregannoOther *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[7];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = oregannoOtherLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOther *oregannoOtherLoadByQuery(struct sqlConnection *conn, char *query)
/* Load all oregannoOther from table that satisfy the query given.  
 * Where query is of the form 'select * from example where something=something'
 * or 'select example.* from example, anotherTable where example.something = 
 * anotherTable.something'.
 * Dispose of this with oregannoOtherFreeList(). */
{
struct oregannoOther *list = NULL, *el;
struct sqlResult *sr;
char **row;

sr = sqlGetResult(conn, query);
while ((row = sqlNextRow(sr)) != NULL)
    {
    el = oregannoOtherLoad(row);
    slAddHead(&list, el);
    }
slReverse(&list);
sqlFreeResult(&sr);
return list;
}

void oregannoOtherSaveToDb(struct sqlConnection *conn, struct oregannoOther *el, char *tableName, int updateSize)
/* Save oregannoOther as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size
 * of a string that would contain the entire query. Arrays of native types are
 * converted to comma separated strings and loaded as such, User defined types are
 * inserted as NULL. Note that strings must be escaped to allow insertion into the database.
 * For example "autosql's features include" --> "autosql\'s features include" 
 * If worried about this use oregannoOtherSaveToDbEscaped() */
{
struct dyString *update = newDyString(updateSize);
dyStringPrintf(update, "insert into %s values ( %u,'%s',%u,%u,'%s','%s','%s')", 
	tableName,  el->bin,  el->chrom,  el->chromStart,  el->chromEnd,  el->id,  el->strand,  el->name);
sqlUpdate(conn, update->string);
freeDyString(&update);
}

void oregannoOtherSaveToDbEscaped(struct sqlConnection *conn, struct oregannoOther *el, char *tableName, int updateSize)
/* Save oregannoOther as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size.
 * of a string that would contain the entire query. Automatically 
 * escapes all simple strings (not arrays of string) but may be slower than oregannoOtherSaveToDb().
 * For example automatically copies and converts: 
 * "autosql's features include" --> "autosql\'s features include" 
 * before inserting into database. */ 
{
struct dyString *update = newDyString(updateSize);
char  *chrom, *id, *strand, *name;
chrom = sqlEscapeString(el->chrom);
id = sqlEscapeString(el->id);
strand = sqlEscapeString(el->strand);
name = sqlEscapeString(el->name);

dyStringPrintf(update, "insert into %s values ( %u,'%s',%u,%u,'%s','%s','%s')", 
	tableName, el->bin ,  chrom, el->chromStart , el->chromEnd ,  id,  strand,  name);
sqlUpdate(conn, update->string);
freeDyString(&update);
freez(&chrom);
freez(&id);
freez(&strand);
freez(&name);
}

struct oregannoOther *oregannoOtherCommaIn(char **pS, struct oregannoOther *ret)
/* Create a oregannoOther out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new oregannoOther */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->bin = sqlUnsignedComma(&s);
ret->chrom = sqlStringComma(&s);
ret->chromStart = sqlUnsignedComma(&s);
ret->chromEnd = sqlUnsignedComma(&s);
ret->id = sqlStringComma(&s);
sqlFixedStringComma(&s, ret->strand, sizeof(ret->strand));
ret->name = sqlStringComma(&s);
*pS = s;
return ret;
}

void oregannoOtherFree(struct oregannoOther **pEl)
/* Free a single dynamically allocated oregannoOther such as created
 * with oregannoOtherLoad(). */
{
struct oregannoOther *el;

if ((el = *pEl) == NULL) return;
freeMem(el->chrom);
freeMem(el->id);
freeMem(el->name);
freez(pEl);
}

void oregannoOtherFreeList(struct oregannoOther **pList)
/* Free a list of dynamically allocated oregannoOther's */
{
struct oregannoOther *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    oregannoOtherFree(&el);
    }
*pList = NULL;
}

void oregannoOtherOutput(struct oregannoOther *el, FILE *f, char sep, char lastSep) 
/* Print out oregannoOther.  Separate fields with sep. Follow last field with lastSep. */
{
fprintf(f, "%u", el->bin);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->chrom);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%u", el->chromStart);
fputc(sep,f);
fprintf(f, "%u", el->chromEnd);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->id);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->strand);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->name);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

void oregannoOtherAttrStaticLoad(char **row, struct oregannoOtherAttr *ret)
/* Load a row from oregannoOtherAttr table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->id = row[0];
ret->attribute = row[1];
ret->attrVal = row[2];
}

struct oregannoOtherAttr *oregannoOtherAttrLoad(char **row)
/* Load a oregannoOtherAttr from row fetched with select * from oregannoOtherAttr
 * from database.  Dispose of this with oregannoOtherAttrFree(). */
{
struct oregannoOtherAttr *ret;

AllocVar(ret);
ret->id = cloneString(row[0]);
ret->attribute = cloneString(row[1]);
ret->attrVal = cloneString(row[2]);
return ret;
}

struct oregannoOtherAttr *oregannoOtherAttrLoadAll(char *fileName) 
/* Load all oregannoOtherAttr from a whitespace-separated file.
 * Dispose of this with oregannoOtherAttrFreeList(). */
{
struct oregannoOtherAttr *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileRow(lf, row))
    {
    el = oregannoOtherAttrLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOtherAttr *oregannoOtherAttrLoadAllByChar(char *fileName, char chopper) 
/* Load all oregannoOtherAttr from a chopper separated file.
 * Dispose of this with oregannoOtherAttrFreeList(). */
{
struct oregannoOtherAttr *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[3];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = oregannoOtherAttrLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOtherAttr *oregannoOtherAttrLoadByQuery(struct sqlConnection *conn, char *query)
/* Load all oregannoOtherAttr from table that satisfy the query given.  
 * Where query is of the form 'select * from example where something=something'
 * or 'select example.* from example, anotherTable where example.something = 
 * anotherTable.something'.
 * Dispose of this with oregannoOtherAttrFreeList(). */
{
struct oregannoOtherAttr *list = NULL, *el;
struct sqlResult *sr;
char **row;

sr = sqlGetResult(conn, query);
while ((row = sqlNextRow(sr)) != NULL)
    {
    el = oregannoOtherAttrLoad(row);
    slAddHead(&list, el);
    }
slReverse(&list);
sqlFreeResult(&sr);
return list;
}

void oregannoOtherAttrSaveToDb(struct sqlConnection *conn, struct oregannoOtherAttr *el, char *tableName, int updateSize)
/* Save oregannoOtherAttr as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size
 * of a string that would contain the entire query. Arrays of native types are
 * converted to comma separated strings and loaded as such, User defined types are
 * inserted as NULL. Note that strings must be escaped to allow insertion into the database.
 * For example "autosql's features include" --> "autosql\'s features include" 
 * If worried about this use oregannoOtherAttrSaveToDbEscaped() */
{
struct dyString *update = newDyString(updateSize);
dyStringPrintf(update, "insert into %s values ( '%s','%s','%s')", 
	tableName,  el->id,  el->attribute,  el->attrVal);
sqlUpdate(conn, update->string);
freeDyString(&update);
}

void oregannoOtherAttrSaveToDbEscaped(struct sqlConnection *conn, struct oregannoOtherAttr *el, char *tableName, int updateSize)
/* Save oregannoOtherAttr as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size.
 * of a string that would contain the entire query. Automatically 
 * escapes all simple strings (not arrays of string) but may be slower than oregannoOtherAttrSaveToDb().
 * For example automatically copies and converts: 
 * "autosql's features include" --> "autosql\'s features include" 
 * before inserting into database. */ 
{
struct dyString *update = newDyString(updateSize);
char  *id, *attribute, *attrVal;
id = sqlEscapeString(el->id);
attribute = sqlEscapeString(el->attribute);
attrVal = sqlEscapeString(el->attrVal);

dyStringPrintf(update, "insert into %s values ( '%s','%s','%s')", 
	tableName,  id,  attribute,  attrVal);
sqlUpdate(conn, update->string);
freeDyString(&update);
freez(&id);
freez(&attribute);
freez(&attrVal);
}

struct oregannoOtherAttr *oregannoOtherAttrCommaIn(char **pS, struct oregannoOtherAttr *ret)
/* Create a oregannoOtherAttr out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new oregannoOtherAttr */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->id = sqlStringComma(&s);
ret->attribute = sqlStringComma(&s);
ret->attrVal = sqlStringComma(&s);
*pS = s;
return ret;
}

void oregannoOtherAttrFree(struct oregannoOtherAttr **pEl)
/* Free a single dynamically allocated oregannoOtherAttr such as created
 * with oregannoOtherAttrLoad(). */
{
struct oregannoOtherAttr *el;

if ((el = *pEl) == NULL) return;
freeMem(el->id);
freeMem(el->attribute);
freeMem(el->attrVal);
freez(pEl);
}

void oregannoOtherAttrFreeList(struct oregannoOtherAttr **pList)
/* Free a list of dynamically allocated oregannoOtherAttr's */
{
struct oregannoOtherAttr *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    oregannoOtherAttrFree(&el);
    }
*pList = NULL;
}

void oregannoOtherAttrOutput(struct oregannoOtherAttr *el, FILE *f, char sep, char lastSep) 
/* Print out oregannoOtherAttr.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->id);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->attribute);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->attrVal);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

void oregannoOtherLinkStaticLoad(char **row, struct oregannoOtherLink *ret)
/* Load a row from oregannoOtherLink table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->id = row[0];
ret->attribute = row[1];
ret->raKey = row[2];
ret->attrAcc = row[3];
}

struct oregannoOtherLink *oregannoOtherLinkLoad(char **row)
/* Load a oregannoOtherLink from row fetched with select * from oregannoOtherLink
 * from database.  Dispose of this with oregannoOtherLinkFree(). */
{
struct oregannoOtherLink *ret;

AllocVar(ret);
ret->id = cloneString(row[0]);
ret->attribute = cloneString(row[1]);
ret->raKey = cloneString(row[2]);
ret->attrAcc = cloneString(row[3]);
return ret;
}

struct oregannoOtherLink *oregannoOtherLinkLoadAll(char *fileName) 
/* Load all oregannoOtherLink from a whitespace-separated file.
 * Dispose of this with oregannoOtherLinkFreeList(). */
{
struct oregannoOtherLink *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileRow(lf, row))
    {
    el = oregannoOtherLinkLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOtherLink *oregannoOtherLinkLoadAllByChar(char *fileName, char chopper) 
/* Load all oregannoOtherLink from a chopper separated file.
 * Dispose of this with oregannoOtherLinkFreeList(). */
{
struct oregannoOtherLink *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = oregannoOtherLinkLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct oregannoOtherLink *oregannoOtherLinkLoadByQuery(struct sqlConnection *conn, char *query)
/* Load all oregannoOtherLink from table that satisfy the query given.  
 * Where query is of the form 'select * from example where something=something'
 * or 'select example.* from example, anotherTable where example.something = 
 * anotherTable.something'.
 * Dispose of this with oregannoOtherLinkFreeList(). */
{
struct oregannoOtherLink *list = NULL, *el;
struct sqlResult *sr;
char **row;

sr = sqlGetResult(conn, query);
while ((row = sqlNextRow(sr)) != NULL)
    {
    el = oregannoOtherLinkLoad(row);
    slAddHead(&list, el);
    }
slReverse(&list);
sqlFreeResult(&sr);
return list;
}

void oregannoOtherLinkSaveToDb(struct sqlConnection *conn, struct oregannoOtherLink *el, char *tableName, int updateSize)
/* Save oregannoOtherLink as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size
 * of a string that would contain the entire query. Arrays of native types are
 * converted to comma separated strings and loaded as such, User defined types are
 * inserted as NULL. Note that strings must be escaped to allow insertion into the database.
 * For example "autosql's features include" --> "autosql\'s features include" 
 * If worried about this use oregannoOtherLinkSaveToDbEscaped() */
{
struct dyString *update = newDyString(updateSize);
dyStringPrintf(update, "insert into %s values ( '%s','%s','%s','%s')", 
	tableName,  el->id,  el->attribute,  el->raKey,  el->attrAcc);
sqlUpdate(conn, update->string);
freeDyString(&update);
}

void oregannoOtherLinkSaveToDbEscaped(struct sqlConnection *conn, struct oregannoOtherLink *el, char *tableName, int updateSize)
/* Save oregannoOtherLink as a row to the table specified by tableName. 
 * As blob fields may be arbitrary size updateSize specifies the approx size.
 * of a string that would contain the entire query. Automatically 
 * escapes all simple strings (not arrays of string) but may be slower than oregannoOtherLinkSaveToDb().
 * For example automatically copies and converts: 
 * "autosql's features include" --> "autosql\'s features include" 
 * before inserting into database. */ 
{
struct dyString *update = newDyString(updateSize);
char  *id, *attribute, *raKey, *attrAcc;
id = sqlEscapeString(el->id);
attribute = sqlEscapeString(el->attribute);
raKey = sqlEscapeString(el->raKey);
attrAcc = sqlEscapeString(el->attrAcc);

dyStringPrintf(update, "insert into %s values ( '%s','%s','%s','%s')", 
	tableName,  id,  attribute,  raKey,  attrAcc);
sqlUpdate(conn, update->string);
freeDyString(&update);
freez(&id);
freez(&attribute);
freez(&raKey);
freez(&attrAcc);
}

struct oregannoOtherLink *oregannoOtherLinkCommaIn(char **pS, struct oregannoOtherLink *ret)
/* Create a oregannoOtherLink out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new oregannoOtherLink */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->id = sqlStringComma(&s);
ret->attribute = sqlStringComma(&s);
ret->raKey = sqlStringComma(&s);
ret->attrAcc = sqlStringComma(&s);
*pS = s;
return ret;
}

void oregannoOtherLinkFree(struct oregannoOtherLink **pEl)
/* Free a single dynamically allocated oregannoOtherLink such as created
 * with oregannoOtherLinkLoad(). */
{
struct oregannoOtherLink *el;

if ((el = *pEl) == NULL) return;
freeMem(el->id);
freeMem(el->attribute);
freeMem(el->raKey);
freeMem(el->attrAcc);
freez(pEl);
}

void oregannoOtherLinkFreeList(struct oregannoOtherLink **pList)
/* Free a list of dynamically allocated oregannoOtherLink's */
{
struct oregannoOtherLink *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    oregannoOtherLinkFree(&el);
    }
*pList = NULL;
}

void oregannoOtherLinkOutput(struct oregannoOtherLink *el, FILE *f, char sep, char lastSep) 
/* Print out oregannoOtherLink.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->id);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->attribute);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->raKey);
if (sep == ',') fputc('"',f);
fputc(sep,f);
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->attrAcc);
if (sep == ',') fputc('"',f);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

