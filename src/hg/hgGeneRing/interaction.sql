# interaction.sql was originally generated by the autoSql program, which also 
# generated interaction.c and interaction.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#gene network interaction - weighted directed edge 
CREATE TABLE interaction (
    fromX varchar(255) not null,	# from- gene, protein, node, etc.
    toY varchar(255) not null,	# to- gene, protein, node, etc.
    score float not null,	# weighted value for score
              #Indices
    KEY(fromX),
    KEY(toY)
);
