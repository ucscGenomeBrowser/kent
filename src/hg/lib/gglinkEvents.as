table ggLink
" hgcGeneGraph relationship from gene pairs to events"
    (
    string gene1 index;       "Gene pair, symbol1 (first in alphabet)"
    string gene2;       "Gene pair, symbol2 (second in alphabet)"
    blob eventIds;     "comma-separated list of eventIds"
    )
