# Local declaration so that local gold.html is picked up.
track gold override
html gold

# Local declaration so that local gap.html is picked up.
track gap override
html gap

track bosTau4ChainPileUp
shortLabel bosTau4 pile up
longLabel bosTau4 chain pile up
group x
priority 600
visibility hide
type bigWig 1 14674
