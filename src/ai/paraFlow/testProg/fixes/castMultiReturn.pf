
string s = "This is a test string.";
print(s);
long pos, string w;

(w,pos) = s.nextWord(0);
to countDown() into int c
   {
   static int count = 11;
   static string s = "Some static string";
   count -= 1;
   c = count;
   }


foreach (int pos in countDown())
    print(pos);

foreach ((string w,int pos) in s.nextWord(pos))
    print(w);
