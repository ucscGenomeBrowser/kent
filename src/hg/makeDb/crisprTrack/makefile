include ../../../inc/common.mk

all:
	cp *.py *.tab *.as /hive/data/outside/crisprTrack/scripts/
	cp -R lib /hive/data/outside/crisprTrack/scripts/
	cp doCrispr.sh $(SCRIPTS)

# don't we have a variable for this?
alpha:
	cp doCrispr.sh /cluster/bin/scripts/
