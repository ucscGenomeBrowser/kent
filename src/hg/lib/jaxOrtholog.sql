# jaxOrtholog.sql was originally generated by the autoSql program, which also 
# generated jaxOrtholog.c and jaxOrtholog.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Jackson Lab Mouse Orthologs
CREATE TABLE jaxOrtholog (
    humanSymbol varchar(255) not null,	# Human HUGO symbol
    humanBand varchar(255) not null,	# Human chromosomal location
    mgiId varchar(255) not null,	# Mouse database id
    mouseSymbol varchar(255) not null,	# Mouse human symbol
    mouseChr varchar(255) not null,	# Mouse chromosome
    mouseCm varchar(255) not null,	# Mouse genetic map positionin centimorgans
    mouseBand varchar(255) not null,	# Mouse chromosome band if any
              #Indices
    INDEX(mgiId),
    INDEX(humanSymbol),
    INDEX(mouseSymbol)
);
