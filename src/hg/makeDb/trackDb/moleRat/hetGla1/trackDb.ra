# Local declaration so that local gold.html is picked up.
track gold override
html gold

# Local declaration so that local gap.html is picked up.
track gap override
html gap

track ensGene override
visibility pack
ensemblIdUrl http://www.ensembl.org/Heterocephalus_glaber_male
