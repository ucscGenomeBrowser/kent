class point
    {
    int x,y;
    double z;
    }

class line
    {
    point start,end;
    string name;
    }

line ray = ((1,2,0.12),(3,4,0.34), "Ray");
print(ray);
print(ray.start.x);
print(ray.start.y);
print(ray.start.z);
print(ray.start);
print(ray.end.x);
print(ray.end.y);
print(ray.end.z);
print(ray.end);
print(ray.name);
