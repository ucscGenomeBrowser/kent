/* polyASizes.c was originally generated by the autoSql program, which also 
 * generated polyASizes.h and polyASizes.sql.  This module links the database and
 * the RAM representation of objects. */

#include "common.h"
#include "linefile.h"
#include "dystring.h"
#include "jksql.h"
#include "polyASizes.h"

static char const rcsid[] = "$Id: polyASizes.c,v 1.1 2005/02/05 19:13:33 markd Exp $";

void polyASizesStaticLoad(char **row, struct polyASizes *ret)
/* Load a row from polyASizes table into ret.  The contents of ret will
 * be replaced at the next call to this function. */
{

ret->id = row[0];
ret->seqSize = sqlUnsigned(row[1]);
ret->tailPolyASize = sqlUnsigned(row[2]);
ret->headPolyTSize = sqlUnsigned(row[3]);
}

struct polyASizes *polyASizesLoad(char **row)
/* Load a polyASizes from row fetched with select * from polyASizes
 * from database.  Dispose of this with polyASizesFree(). */
{
struct polyASizes *ret;

AllocVar(ret);
ret->id = cloneString(row[0]);
ret->seqSize = sqlUnsigned(row[1]);
ret->tailPolyASize = sqlUnsigned(row[2]);
ret->headPolyTSize = sqlUnsigned(row[3]);
return ret;
}

struct polyASizes *polyASizesLoadAll(char *fileName) 
/* Load all polyASizes from a whitespace-separated file.
 * Dispose of this with polyASizesFreeList(). */
{
struct polyASizes *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileRow(lf, row))
    {
    el = polyASizesLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct polyASizes *polyASizesLoadAllByChar(char *fileName, char chopper) 
/* Load all polyASizes from a chopper separated file.
 * Dispose of this with polyASizesFreeList(). */
{
struct polyASizes *list = NULL, *el;
struct lineFile *lf = lineFileOpen(fileName, TRUE);
char *row[4];

while (lineFileNextCharRow(lf, chopper, row, ArraySize(row)))
    {
    el = polyASizesLoad(row);
    slAddHead(&list, el);
    }
lineFileClose(&lf);
slReverse(&list);
return list;
}

struct polyASizes *polyASizesCommaIn(char **pS, struct polyASizes *ret)
/* Create a polyASizes out of a comma separated string. 
 * This will fill in ret if non-null, otherwise will
 * return a new polyASizes */
{
char *s = *pS;

if (ret == NULL)
    AllocVar(ret);
ret->id = sqlStringComma(&s);
ret->seqSize = sqlUnsignedComma(&s);
ret->tailPolyASize = sqlUnsignedComma(&s);
ret->headPolyTSize = sqlUnsignedComma(&s);
*pS = s;
return ret;
}

void polyASizesFree(struct polyASizes **pEl)
/* Free a single dynamically allocated polyASizes such as created
 * with polyASizesLoad(). */
{
struct polyASizes *el;

if ((el = *pEl) == NULL) return;
freeMem(el->id);
freez(pEl);
}

void polyASizesFreeList(struct polyASizes **pList)
/* Free a list of dynamically allocated polyASizes's */
{
struct polyASizes *el, *next;

for (el = *pList; el != NULL; el = next)
    {
    next = el->next;
    polyASizesFree(&el);
    }
*pList = NULL;
}

void polyASizesOutput(struct polyASizes *el, FILE *f, char sep, char lastSep) 
/* Print out polyASizes.  Separate fields with sep. Follow last field with lastSep. */
{
if (sep == ',') fputc('"',f);
fprintf(f, "%s", el->id);
if (sep == ',') fputc('"',f);
fputc(sep,f);
fprintf(f, "%u", el->seqSize);
fputc(sep,f);
fprintf(f, "%u", el->tailPolyASize);
fputc(sep,f);
fprintf(f, "%u", el->headPolyTSize);
fputc(lastSep,f);
}

/* -------------------------------- End autoSql Generated Code -------------------------------- */

