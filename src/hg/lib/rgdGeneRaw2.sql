CREATE TABLE rgdGeneRaw2  (
    seqname varchar(40),
    source varchar(255),
    feature varchar(40),
    start  varchar(40),
    end varchar(40),	
    score  varchar(40),		
    strand varchar(40),	
    frame varchar(40),
    rgdId varchar(255),
    KEY(rgdId)
);
