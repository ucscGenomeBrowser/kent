# Local declaration so that local gold.html is picked up.
track gold
shortLabel Assembly
longLabel Assembly from Fragments
group map
priority 10
visibility dense
color 150,100,30
altColor 230,170,40
type bed 3 +

# Local declaration so that local gap.html is picked up.
track gap
shortLabel Gap
longLabel Gap Locations
priority 11
group map
visibility dense
type bed 3 +

# Local declaration so that local ctgPos2.html is picked up.
track ctgPos2
shortLabel Contigs
longLabel Assembly Contigs
group map
priority 10
visibility dense
type ctgPos

#Overrides from visibility.ra

track gc5Base override
visibility full

track xenoRefGene override
visibility pack

track intronEst override
visibility hide

