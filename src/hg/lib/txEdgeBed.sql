# txEdgeBed.sql was originally generated by the autoSql program, which also 
# generated txEdgeBed.c and txEdgeBed.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#A transcription edge with first fields bed format.
CREATE TABLE txEdgeBed (
    chrom varchar(255) not null,	# Chromosome or contig name
    chromStart int not null,	# Start position, zero-based
    chromEnd int not null,	# End position, non-inclusive
    name varchar(255) not null,	# Name of evidence supporting edge
    score int not null,	# Score - 0-1000
    strand char(1) not null,	# Strand - either plus or minus
    startType char(1) not null,	# [ or ( for hard or soft
    type enum("exon", "intron") not null,	# edge type
    endType char(1) not null,	# ] or ) for hard or soft
              #Indices
    INDEX(name(12))
);
