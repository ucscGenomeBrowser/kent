# required db variable
db papAnu3

# remove the first .1 from the contig names in the Ensembl GTF file
# add chr to the chrom names
# nameTranslation "s/^\([0-9XY][0-9]*\)/chr\1/; s/\.1//"

liftUp /hive/data/genomes/papAnu3/jkStuff/ensToUcsc.lift
