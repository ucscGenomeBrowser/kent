# gencodeIntron.sql was originally generated by the autoSql program, which also 
# generated gencodeIntron.c and gencodeIntron.h.  This creates the database representation of
# an object which can be loaded and saved from RAM in a fairly 
# automatic way.

#Gencode intron status
CREATE TABLE gencodeIntron (
    chrom varchar(255) not null,	# Chromosome or FPC contig
    chromStart int unsigned not null,	# Start position in chromosome
    chromEnd int unsigned not null,	# End position in chromosome
    name varchar(255) not null,	# Intron_id from GTF
    enum('not_tested', 'RT_positive','RT_negative','RT_wrong_junction', 'RT_submitted', 'RACE_validated' )
    strand char(1) not null,	# + or -
    transcript varchar(255) not null,	# Transcript_id from GTF
    geneId varchar(255) not null,	# Gene_id from GTF
              #Indices
    PRIMARY KEY(chrom)
);
