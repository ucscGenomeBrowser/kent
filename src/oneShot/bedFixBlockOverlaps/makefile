include ../../inc/common.mk
SCRIPT=bedFixBlockOverlaps

#cannot use common include rules, don't know how to overwrite :: targets
#include ../../inc/cgi_build_rules.mk

my::
	rm -f ${BINDIR}/${SCRIPT}
	cp -p ${SCRIPT} ${BINDIR}/
beta::
	rm -f ${BINDIR}$/${SCRIPT}
	cp -p ${SCRIPT} ${BINDIR}/
alpha::
	rm -f ${BINDIR}/${SCRIPT}
	cp -p ${SCRIPT} ${BINDIR}/
clean::

compile::

install::
	rm -f ${BINDIR}/${SCRIPT}
	cp -p ${SCRIPT} ${BINDIR}/
